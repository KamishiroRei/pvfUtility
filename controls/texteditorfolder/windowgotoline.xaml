<?xml version="1.0" encoding="utf-8"?>
<local:WindowGotoLine xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="clr-namespace:PvfCode.Controls;assembly=pvfUtility" xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" xmlns:local="clr-namespace:PvfCode.Controls.TextEditorFolder;assembly=pvfUtility" Window.Title="跳转到偏移量" FrameworkElement.Width="300" FrameworkElement.Height="180">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <controls:EditBox Caption="偏移量：" NullText="请输入字符偏移量" Value="{Binding Offset, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource AncestorType=local:WindowGotoLine, Mode=FindAncestor}}" />
    <StackPanel Margin="0,10,0,10" HorizontalAlignment="Right" Orientation="Horizontal">
      <Button Margin="0,0,10,0" Content="{DynamicResource common_OK}">
      </Button>
      <Button Content="{DynamicResource common_Cancel}">
      </Button>
    </StackPanel>
  </Grid>
</local:WindowGotoLine>
