<?xml version="1.0" encoding="utf-8"?>
<pvfcode:WindowLoading xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:appsetting="clr-namespace:PvfCode;assembly=PvfCode.Models" xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" FrameworkElement.MinWidth="300" Control.Padding="10" Window.ResizeMode="NoResize" Window.SizeToContent="WidthAndHeight" Window.Topmost="False" Window.WindowStyle="None" xmlns:pvfcode="clr-namespace:PvfCode;assembly=pvfUtility" Window.Title="{Binding Source={x:Static appsetting:AppSetting.Instance}, Path=AppName}" Control.FontSize="{Binding Source={x:Static appsetting:AppSetting.Instance}, Path=FontSize}">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="auto" />
      <RowDefinition Height="auto" />
      <RowDefinition Height="auto" />
    </Grid.RowDefinitions>
    <Label Content="{Binding Source={x:Static appsetting:AppSetting.Instance}, Path=AppName}" />
    <TextBlock Name="title" Grid.Row="1" Margin="0,10,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" Text="{Binding Text}">
    </TextBlock>
    <Grid Row="2" Margin="0,10,0,0" VerticalAlignment="Bottom">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="auto" />
      </Grid.ColumnDefinitions>
      <ProgressBar Height="18" Background="{DynamicResource LoadingProgressBarBackBrush}" BorderThickness="0" Foreground="{DynamicResource LoadingProgressBarForeBrush}" IsIndeterminate="True" BorderBrush="{x:Null}" />
      <Button Grid.Column="1" Margin="10,0,0,0" Content="{DynamicResource common_Stop}" Cursor="Hand" Visibility="{Binding ShowStopButton, Converter={StaticResource ConvertBoolenToVisibility}}" />
    </Grid>
  </Grid>
</pvfcode:WindowLoading>