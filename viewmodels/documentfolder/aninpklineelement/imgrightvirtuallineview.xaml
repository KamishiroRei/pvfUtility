<?xml version="1.0" encoding="utf-8"?>
<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:appsetting="clr-namespace:PvfCode;assembly=PvfCode.Models" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" Margin="20,0,0,-10" FontFamily="微软雅黑" xmlns:aninpklineelement="clr-namespace:PvfCode.ViewModels.DocumentFolder.AniNpkLineElement;assembly=pvfUtility">
  <StackPanel Orientation="Horizontal">
    <Button Margin="5" Content="{DynamicResource ImgRightVirtualLineView_PreviewBtnTitle}" Cursor="Hand" ToolTipService.InitialShowDelay="50" Command="{Binding OnPreviewCommand}">
      <Control.Template>
        <ControlTemplate TargetType="{x:Type Button}">
          <TextBlock>
            <ContentPresenter />
            <FrameworkElement.Style>
              <Style TargetType="{x:Type TextBlock}">
                <Style.Triggers>
                  <Trigger Property="UIElement.IsMouseOver" Value="true">
                    <Setter Property="TextDecorations" Value="Underline" />
                  </Trigger>
                </Style.Triggers>
              </Style>
            </FrameworkElement.Style>
          </TextBlock>
        </ControlTemplate>
      </Control.Template>
      <FrameworkElement.Style>
        <Style TargetType="{x:Type Button}">
          <Setter Property="Foreground" Value="{Binding Source={x:Static appsetting:AppSetting.Instance}, Path=EditConfig.NowEditorHighlightingColorOption.LstItemNameForeBrush, Mode=OneWay}" />
          <Style.Triggers>
            <Trigger Property="UIElement.IsMouseOver" Value="true">
              <Setter Property="Foreground" Value="#007acc" />
            </Trigger>
          </Style.Triggers>
        </Style>
      </FrameworkElement.Style>
    </Button>
    <Button Margin="5" Content="{DynamicResource ImgRightVirtualLineView_GoTo}" Cursor="Hand" ToolTipService.InitialShowDelay="50" Command="{Binding OnGoToFileCommand}">
      <Control.Template>
        <ControlTemplate TargetType="{x:Type Button}">
          <TextBlock>
            <ContentPresenter />
            <FrameworkElement.Style>
              <Style TargetType="{x:Type TextBlock}">
                <Style.Triggers>
                  <Trigger Property="UIElement.IsMouseOver" Value="true">
                    <Setter Property="TextDecorations" Value="Underline" />
                  </Trigger>
                </Style.Triggers>
              </Style>
            </FrameworkElement.Style>
          </TextBlock>
        </ControlTemplate>
      </Control.Template>
      <FrameworkElement.Style>
        <Style TargetType="{x:Type Button}">
          <Setter Property="Foreground" Value="{Binding Source={x:Static appsetting:AppSetting.Instance}, Path=EditConfig.NowEditorHighlightingColorOption.LstItemNameForeBrush, Mode=OneWay}" />
          <Style.Triggers>
            <Trigger Property="UIElement.IsMouseOver" Value="true">
              <Setter Property="Foreground" Value="#007acc" />
            </Trigger>
          </Style.Triggers>
        </Style>
      </FrameworkElement.Style>
    </Button>
  </StackPanel>
</UserControl>