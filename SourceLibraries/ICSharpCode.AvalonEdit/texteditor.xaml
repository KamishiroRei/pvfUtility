<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:AvalonEdit="clr-namespace:ICSharpCode.AvalonEdit" xmlns:editing="clr-namespace:ICSharpCode.AvalonEdit.Editing">
  <Style x:Key="{x:Type AvalonEdit:TextEditor}" TargetType="{x:Type AvalonEdit:TextEditor}">
    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" />
    <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type AvalonEdit:TextEditor}">
          <Border Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}">
            <ScrollViewer Name="PART_ScrollViewer" Focusable="False" CanContentScroll="True" VerticalScrollBarVisibility="{TemplateBinding AvalonEdit:TextEditor.VerticalScrollBarVisibility}" HorizontalScrollBarVisibility="{TemplateBinding AvalonEdit:TextEditor.HorizontalScrollBarVisibility}" VerticalContentAlignment="Top" HorizontalContentAlignment="Left" Padding="{TemplateBinding Control.Padding}" Content="{Binding Path=TextArea, RelativeSource={RelativeSource TemplatedParent}}" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="AvalonEdit:TextEditor.WordWrap" Value="True">
              <Setter TargetName="PART_ScrollViewer" Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{x:Type editing:TextArea}" TargetType="{x:Type editing:TextArea}">
    <Setter Property="FrameworkElement.FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="editing:TextArea.SelectionBrush">
      <Setter.Value>
        <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.HighlightColorKey}}" Opacity="0.7" />
      </Setter.Value>
    </Setter>
    <Setter Property="editing:TextArea.SelectionBorder">
      <Setter.Value>
        <Pen Brush="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" Thickness="1" />
      </Setter.Value>
    </Setter>
    <Setter Property="editing:TextArea.SelectionForeground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type editing:TextArea}">
          <DockPanel Focusable="False">
            <ItemsControl DockPanel.Dock="Left" Focusable="False" ItemsSource="{Binding Path=LeftMargins, RelativeSource={RelativeSource TemplatedParent}}">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                  <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
            </ItemsControl>
            <ContentPresenter Panel.ZIndex="-1" Focusable="False" Content="{Binding Path=TextView, RelativeSource={RelativeSource TemplatedParent}}" />
          </DockPanel>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>