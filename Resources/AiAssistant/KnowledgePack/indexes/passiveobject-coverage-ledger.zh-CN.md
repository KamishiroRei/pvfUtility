# PassiveObject / AttackInfo / Hitbox 覆盖账本

状态：需验证

本文记录主目标 PVF 中 `passiveobject/` 主线的只读覆盖进度。它用于告诉 Agent 下一步查哪里、哪些块已经有主目标样本、哪些结论仍不能由静态只读证明。

治理提示：本账本已超过约 100KB，后续新增小桶只写短结论、边界和路由行；长坐标、长创建链、同族批量差异写入分片索引。落账前先读 `passiveobject-ledger-governance.zh-CN.md`。

## 入口规则

| 问题 | 结论 |
| --- | --- |
| 数字 passiveobject ID 怎么查？ | 先走 `passiveobject/passiveobject.lst`。不要用数字形状猜类型。 |
| 辅助对照能不能写成正式结论？ | 不能。辅助对照只能提示差异和补充观察，主目标结论必须回主目标单独复核。 |
| `.obj -> .act/.ani/.atk` 怎么跟？ | 从当前 `.obj` 所在目录解析相对路径；`.act` 中的 ANI 引用继续按 `.act` 目录解析。 |
| Hitbox 能不能只靠 `.atk` 判断？ | 不能。`.atk` 描述攻击信息；命中/攻击框需要进入对应 `.ani` 的帧数据查看。 |
| `SearchScript` 未命中能否证明不存在？ | 不能直接证明。二进制 ANI、路径大小写、索引行为都可能影响搜索结果。 |

## 主目标范围账本

| 类别 | 主目标只读观察 | 说明 |
| --- | ---: | --- |
| `passiveobject/passiveobject.lst` registry 条目 | 6548 | passiveobject 数字 ID 的正式解析入口。 |
| `passiveobject/` 文件总量 | 39114 | 文件清单范围，不等于 registry 条目数。 |
| `passiveobject/actionobject/` | 20601 | 最大目录簇，含地图机关、怪物/场景动作对象等。 |
| `passiveobject/character/` | 6235 | 角色公共对象、职业派生对象等。 |
| `passiveobject/mapobject/` | 3649 | 地图物件、可破坏物等。 |
| `passiveobject/monster/` | 1229 | passiveobject registry 下的 monster 前缀文件；不等于 monster registry。 |
| `passiveobject/common/` | 547 | 公共动作、动画、攻击信息等。 |

## 文件名形态账本

以下是文件名包含对应后缀片段的计数，不把它写成严格扩展名统计。

| 片段 | 主目标命中 | 注意 |
| --- | ---: | --- |
| `.obj` | 6984 | passiveobject 对象入口主体。 |
| `.act` | 5492 | 动作、触发、行为、创建链主体。 |
| `.atk` | 2704 | attackinfo 主体。 |
| `.ani` | 22211 | 含 `.ani.als` 侧车文件；不能直接当作纯 ANI 数。 |
| `.ani.als` | 665 | ANI 关联侧车文件，应与二进制 ANI 分开看。 |
| `.ptl` | 1558 | 粒子资源引用边界，静态 PVF 只能证明引用存在。 |
| `.als` | 666 | 多数为 `.ani.als`，仍需按具体路径读。 |
| `.nut` | 0 | 主目标 `passiveobject/` 文件清单未观察到 NUT 文件；NUT 运行边界仍属于独立专题。 |

## 字段命中账本

| 字段 / 块 | 主目标命中 | 已观察列形 | 当前结论 |
| --- | ---: | --- | --- |
| `[object destroy condition] ... [/object destroy condition]` | 1495 | 可为空闭合块；也可含反引号 token 与数值行。 | 是闭合块，需读到结束标签；不能只看起始标签。 |
| `[hp max]` | 883 | 一个数值。 | 结构上是生命上限入口；实机耐久语义未由静态只读证明。 |
| `[hp destroy]` | 908 | 一个数值。 | 结构上是血量销毁入口；`0/1` 的运行含义需实机或更多样本确认。 |
| `[destroy particle]` | 417 | 一个相对 `.ptl` 路径。 | 只能证明销毁粒子引用；不证明客户端资源完整。 |
| `[vanish on move collision]` | 132 | 一个数值。 | 与移动碰撞消失相邻；碰撞判定仍需运行验证。 |
| `[under gravity]` | 3 | 一个数值。 | 少量样本字段，先作为物理/重力入口记录。 |
| `[team]` | 1349 | 一个数值。 | 与归属/阵营相关；不要直接等同伤害敌我规则。 |
| `[homing] ... [/homing]` | 199 | 闭合块，内部有追踪参数；已复核 `[ENEMY]`、`[MONSTER] + registry ID`、`[straight homing]` 三类样本。 | 追踪配置存在；实际追踪轨迹、速度单位和旋转公式需运行验证。 |
| `[homing use]` | 199 | 一个数值。 | 追踪开关样字段。 |
| `[homing follow]` | 199 | 一个反引号 token，如 `[ENEMY]`；`[MONSTER]` 样本后可跟 monster ID。 | 目标类型 token 需原样保留；`[MONSTER] 61194` 走 `monster/monster.lst`，不是 passiveobject ID。 |
| `[homing velocity]` | 199 | 两个数值。 | 速度列形已观察；运动公式未证明。 |
| `[straight homing]` | 1 | 空标签，位于 `[homing]` 块内；主目标唯一命中为 `forest_elf/stone.obj`。 | 只能证明该空标签在主目标样本中存在；辅助对照另有独有 straight homing 命中，不能写成主目标事实。 |
| `[CREATE PASSIVEOBJECT] ... [/CREATE PASSIVEOBJECT]` | 1653 | 闭合块，含 `[INDEX]`、`[PARTICLE FILENAME]`、`[LEVEL]`、`[POS]` 等。 | `[INDEX]` 必须回 `passiveobject/passiveobject.lst` 解析。 |
| `[CREATE PASSIVEOBJECT CIRCLE]` | 0 | 当前样本 `passiveobject/` SearchScript 未命中。 | 只能写成当前搜索范围未观察到，不可写成主目标全局不存在。 |
| `[active status]` | 526 | `.atk` 状态入口；状态 token 后可见三数值列、四数值列、五数值列或七数值列。 | 状态施加结构存在；概率、持续、伤害含义需专项验证。 |
| `[pvp] ... [/pvp]` | 40 | `.atk` 覆盖闭合块，内部可重写 `[hit info]`、`[damage reaction]`、`[damage bonus]` 等局部字段。 | 只证明 PVP 覆盖结构存在，不证明 PVP 结算规则。 |
| `[DAMAGE BOX]` | 脚本文本搜索 0；二进制 ANI 正样本已观察 | 反编译 ANI 后样本为六数值列。 | SearchScript 不能证明二进制 ANI 中不存在；hitbox 结论必须以 ANI 反编译为准。 |

## 主目标样本锚点

| 样本链 | 只读结论 |
| --- | --- |
| `guide_post_manager.obj` | `[object destroy condition]` 可以是空闭合块。 |
| `medusa_down.obj` | `[object destroy condition]` 可含 `[destroy condition]`、`[time limite]` 与数值；同文件还含完整 `[homing]` 块，follow 为 `[ENEMY]`，无 registry ID 行。`medusa_down.ani` / `medusa_attack.ani` 均有攻击框。 |
| `nest.obj` | 8793 回 `passiveobject/passiveobject.lst` 命中 `ActionObject/Act8/Map/pirateonthetrain/nest.obj`，在 `monster/monster.lst` 未命中。对象写 `[hp max] 20000`、`[hp destroy] 0`、`[basic action] Action/nest.act` 与 `[destroy particle]`；action 在 `[ON DAMAGE]` 后执行粒子并 `[DESTROY]`，linked `nest.ani` 11 帧均有同组 `[DAMAGE BOX]`，未观察到 `[ATTACK BOX]`。 |
| `bike1.obj` | 10197 回 `passiveobject/passiveobject.lst` 命中 `ActionObject/Act8/Map/seatrainretaking/bike1.obj`，在 `monster/monster.lst` 未命中。对象无 `.act/.atk`，以 `[basic motion] Animation/bike1.ani` 直连 ANI；同时有 `[hp max] 20000`、`[hp destroy] 1` 与 `[destroy particle]`，linked ANI 单帧有 `[DAMAGE BOX]`，未观察到 `[ATTACK BOX]`。 |
| `deserter_Bomb.obj -> deserter_BombSub.obj` | 9109 回 `passiveobject/passiveobject.lst` 命中 `ActionObject/Common/deserter_Bomb.obj`，父对象写 `[vanish on move collision] 0`、`[basic action] Action/deserter_Bomb.act` 与 `[attack info] AttackInfo/deserter_Bomb.atk`；父 action 在行为 0 创建 9110，9110 回同 registry 命中 `ActionObject/Common/deserter_BombSub.obj`，9109/9110 在 `monster/monster.lst` 未命中。父 `Bomb.ani` 只有 `[DAMAGE BOX]`；子 `fireBomb_normal.ani` 的 FRAME001-003 有 `[ATTACK BOX]`，两个 dodge SUB ANI 未观察到盒字段。 |
| `pub_table.obj` | `[vanish on move collision]`、`[under gravity]`、`[hp destroy]` 与 `[etc action]` 同时出现。 |
| `forest_elf/stone.obj` | `[straight homing]` 位于 `[homing]` 块内，follow 为 `[ENEMY]`；同对象挂 `stone.atk`，`stone.ani` 两帧有攻击框。 |
| `treasurebox.obj -> treasurebox_open.obj` | `[object destroy condition]` 内可用 `[object create after destroy]` 与 `[add object index] 1 9330`；9330 已回 `passiveobject/passiveobject.lst` 命中 `ActionObject/Act8/Map/pirateonthetrain/treasurebox_open.obj`，并在 `monster/monster.lst` 未命中。源 `treasurebox.ani` 有 `[DAMAGE BOX]`，打开后的 `treasurebox_open.ani` 未观察到盒字段。 |
| `junktree.obj / junkwagon.obj` | 8004 回 `passiveobject/passiveobject.lst` 命中 `ActionObject/Common/JunkTree.obj`；8000 回同 registry 命中 `ActionObject/Common/JunkWagon.obj`，两者在 `monster/monster.lst` 均未命中。`tree_thrower` 三个 source action 的 `[CREATE PASSIVEOBJECT] [INDEX] 8004` 分别使用 center/down/up 粒子创建同一树对象。`JunkTree.obj` 有 `[object destroy condition] [destroy condition] [on end of animation]`、`[vanish on move collision] 0`、`[hp max] 5500`、`[hp destroy] 1`，并复用 `attackInfo/JunkWagon.atk`。兄弟 `JunkWagon.obj` 也复用同一 `.atk`，但其销毁在 action 行为中以粒子加 `[DESTROY]` 表达。`JunkTree.ani` 前两帧有攻击盒，后三帧有 damage box；`JunkWagon.ani` 两帧循环，每帧有三条攻击盒和一条 damage box。 |
| `mind_control_octo.obj` | `[homing follow] [MONSTER] 61194` 已回 `monster/monster.lst` 命中 `Act8/congcong/congcong.mob`，并在 `passiveobject/passiveobject.lst` 未命中；本体 basic / last ANI 只有 damage box，未观察到攻击框。 |
| `enginecover_1.act` | `[CREATE PASSIVEOBJECT]` 出现在 `[BEHAVIOR]` 内；样本 `[INDEX] 8777` 已回 passiveobject registry 解析。 |
| `enginecover_crochan2.act -> _enginecover_2.ani` | `[BASE ANI]` 链路可闭合到二进制 ANI；样本帧中观察到 `[DAMAGE BOX]` 六数值列。 |
| `enginecover_1 <-> enginecover_2` | `[CREATE PASSIVEOBJECT]` 样本可形成静态双对象回环；运行时是否循环需看触发和销毁。 |
| `enginecover_crochan -> enginecover_crochan2 -> enginecover_crochan` | 创建链可回到源对象，并可分支到 `ActionObject/Monster/` 前缀 passiveobject。 |
| `[CREATE PASSIVEOBJECT] [INDEX] [RANDOM SELECT]` | `8718/8719/8720`、`8679/8680`、`9136/9137`、`16031/16052/16053` 已回 passiveobject registry 命中；这些是随机 passiveobject ID 候选，不是行为编号。 |
| `summonstonebody.act` | `[CREATE PASSIVEOBJECT] [INDEX] 8568` 走 passiveobject registry，命中 `ActionObject/Common/GuardianGrenadeNoneBomb.obj`；同一行为块后续 `[SUMMON MONSTER] [INDEX] [RANDOM SELECT] 61129-61133` 走 monster registry，是防误判样本。8568 下游对象挂 `GuardianGrenadeExplosion.atk`，BASE `GrenadeNoneBomb.ani` 有攻击框。 |
| `CREATE PASSIVEOBJECT` 空/内联检索 | 创建块 1653；创建块后 120 字符内带 `[INDEX]` 也为 1653；当前 `passiveobject/` SearchScript 范围未观察到空 `[INDEX]` 或省略 `[INDEX]` 的内联创建列形。 |
| `arrow.atk` / `herobubble.atk` / `snowball1.atk` / `boom.atk` / `boom2.atk` | `[active status]` 下可见不同状态 token，状态行长度可为三数值列、四数值列、五数值列或七数值列。 |
| `atpoisonflask.atk` / `energyball.atk` / `bulletbowgun.atk` | `.atk` 中 `[pvp] ... [/pvp]` 可覆盖 `[hit info]`、`[damage reaction]` 或 `[damage bonus]` 等不同字段。 |
| 40 个主目标 `.atk [pvp]` 文件 | 已全量读取并盘点块内字段集合；见 `indexes/attackinfo-pvp-block-inventory.zh-CN.md`。 |
| `ATPoisonFlask` / `BulletBowGun` / `OutRageBreakFloor` / `fire_boom` / `RevengeThunderBolt` | PVP `.atk` 已有触发链试点闭合到 `.obj/.act/.ani`，部分 ANI 观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`。 |
| `common/` 5 个 BASE ANI 动作 | `golgothunder.act` / `golgothunder_item.act` 共用 `GolgoThunder.ani` 正样本；`title_mega.act` 的 `Title_mega.ani` 是正样本；`golgothunder_summon.act`、`title_mega_drop.act` 的 BASE/SUB ANI 未观察到盒字段。 |
| `mapobject/breakableobject/actionbrazier.obj` | mapobject 可走 `.obj [basic motion]` 直连 ANI；`BrazierFront.ani` 观察到 `[DAMAGE BOX]`，同对象 etc motion `BrazierBack.ani` / `BrazierLight.ani` 未观察到盒字段。 |
| `mapobject/breakableobject` 5 对象扩样 | `actionfountain`、`actionstonepillar`、`eventbarrier_1`、`actiontorchilght`、`darkflower` 的 basic motion 均有 `[DAMAGE BOX]` 正样本；部分 etc motion 有 damage box，部分未观察到盒字段。 |
| `character/mage/fluorecollider*.act` | `start` 创建 90010005，`stay` 创建 90010006 / 90010007 / 90010009；这些高位 ID 在主目标 passiveobject registry 与全 registry 均未命中，必须保留未闭合风险。`end` 同一行为块三次创建 23055，命中 `Character/Mage/FluoreColliderNoneExplosion.obj`，直连 `Common/Animation/NoneExplosion.ani`；主目标 `NoneExplosion.ani` 空图帧有 `[ATTACK BOX]`。`stay_body.ani` 有 `[DAMAGE BOX]`，部分 start/end/SUB 样本未观察到盒字段。 |
| `character/mage/atconvergencecannonexplosion.act` | 主目标 action 本体可读，`[BASE ANI]` 指向 `../Animation/ATConvergenceCannon/Explosion/Explosion_Dodge.ani`，`[SUB ANI]` 为空闭合块，0 到 8 帧均执行 `[ATTACKRECT] [RESET]` 行为；主目标直接读取该 BASE ANI 未命中，同目录 `Explosion.ani` 也未命中，登记为引用断链 / 未展开风险。辅助对照同路径 ANI 可读且有攻击盒，只能作为目标集差异提示。 |
| `actionobject/act8/map/seatrainretaking` 4 个 SUB ANI 动作 | `brokenwindow1`、`torch`、`merman` 的 BASE/SUB ANI 未观察到盒字段；`gt96002` 的 BASE ANI 有 `[DAMAGE TYPE] SUPERARMOR` 与 `[DAMAGE BOX]`，其 SUB ANI 未观察到盒字段。 |
| `character/common/olympicfairyshield.obj` | character/common 可通过 `.obj [basic motion]` 和 `[etc motion]` 直连 ANI；已读三个 ANI 样本均观察到 `[ATTACK BOX]` 六数值列。 |
| `character/common/fireexplosioncreater.obj -> 48198` | 父对象走 `[basic action]`，父 ANI 未观察到盒字段；action 创建 48198，已回 passiveobject registry 命中 `Common/FireExplosionItemAttack8.obj`，下游 `FireExplosion.ani` 有 `[ATTACK BOX]`。 |
| `character/fighter/atenergyball.obj` | `.obj` 同时有 `[basic motion]`、`[etc motion]`、`[attack info]` 与 `[etc attack info]`；basic / charge ANI 有攻击框，exp ANI 仅部分帧观察到攻击框。 |
| `character/gunner/atnapalmbomb.obj` | bullet、area1、area2 三个 owner-listed ANI 抽样未观察到盒字段；记录为静态无盒样本，不解释运行伤害或区域命中。 |
| `character/gunner/atsteyrfireexplosion.obj` | owner 借用 `../../Common/Animation/FireExplosion.ani`，该 common ANI 有 `[ATTACK BOX]`；相对路径必须从 owner 目录解析。 |
| `actionobject/act8/map/pirateship` 多层创建链 | `pirate_ship -> fire -> pirate_bomb_drop -> pirate_ship_bomb` 三层创建 ID 均回 passiveobject registry 命中；前几层 ANI 未观察到盒字段，末层爆炸对象已有 `[ATTACK BOX]` 正样本。 |
| `actionobject/monster/` 文件清单 | 主目标该前缀下观察到 10980 个文件，是 actionobject 最大分桶；路径含 `monster` 只是 passiveobject/actionobject 内部前缀，不等同于 Monster 主线。 |
| `actionobject/monster/powerstation/kohlepowerstation` 的 `fitz/boom` 与 `slime/boom` | 两个 owner 均写 `[attack info] attackinfo/boom.atk` 和 `[basic action] Action/boom.act`，但按 owner 目录解析到不同物理 `.atk/.act/.ani`。`fitz/boom.atk` 为 `[absolute damage] 4500`、burn 七数值列，`boom.act` 指向 `boom.ani`，`FRAME021` 到 `FRAME025` 有 `[ATTACK BOX]`；`slime/boom.atk` 为 `[damage bonus] 100`、burn 七数值列，`boom.act` 指向 `boom01.ani` / `boom02.ani`，`boom01` 前 4 帧有攻击框，`boom02` 未观察到攻击框。辅助对照同路径同形。 |
| `equipmentpassiveobject/requiem/003/boom` | owner 也写 `[attack info] attackinfo/boom.atk` 和 `[basic action] Action/boom.act`，按 requiem/003 目录解析到独立物理 `.atk/.act/.ani`。`.atk` 为 `[damage bonus] 100`、burn 七数值列；`boom.act` 除 BASE/SUB ANI 与销毁行为外，还在 `[LAST ATTACKSUCCESS]` 触发下执行 `[CHECKUP OBJECT] 1`，行为 1 通过 `[APPENDAGE] 7000` 附加 `[equipment physical defense] [%] -10` 与 `[equipment magical defense] [%] -10`。`boom01.ani` 前 4 帧有攻击框，`boom02.ani` 未观察到盒字段。辅助对照同路径同形。 |
| `actionobject/monster/chiefmong/boom` | owner 写 `[attack info] AttackInfo/Boom.atk` 和 `[basic action] Action/Boom.act`，大小写按主目标原文保留；全 `passiveobject/` owner 搜索中该大小写字符串仅命中 chiefmong。`.atk` 为 `[damage bonus] 100`、physic/dark/down，含 `[active status] [curse] 100 70 5000 30 200 20 200` 七数值列；`Boom.act [BASE ANI] -> Boom.ani` 的 `FRAME000` 到 `FRAME002` 有 `[ATTACK BOX]`，`[SUB ANI] -> Boom2.ani` 未观察到盒字段。辅助对照同路径同形。 |
| `boom2.atk` 3 个物理样本 | 主目标 `SearchFileName boom2.atk` 命中 chiefmong、slime 与 requiem/003 三个物理 `.atk`；其中 `AttackInfo/Boom2.atk` owner 字符串只命中 `chiefmong/boom_flu.obj`，`attackinfo/boom2.atk` owner 字符串命中 slime/requiem 两个 `boom2.obj`。 |
| `actionobject/monster/chiefmong/boom_flu` | owner 写 `[basic action] Action/Boom2.act` 与 `[attack info] AttackInfo/Boom2.atk`。`.atk` 为 `[absolute damage] 500`、physic/dark、未观察到 `[active status]`；`Boom2.act [BASE ANI] -> Boom3.ani`，`FRAME001` 到 `FRAME003` 有 `[ATTACK BOX]`，其余样本帧未观察到攻击框。辅助对照同路径同形。 |
| `actionobject/monster/powerstation/kohlepowerstation/slime/boom2` | owner 写 `[basic action] Action/boom.act` 与 `[attack info] attackinfo/boom2.atk`，复用 slime 目录 `boom.act/boom01.ani/boom02.ani`。`.atk` 同时有 `[damage] 3000`、`[absolute damage] 500`、`[damage bonus] 100`，并有 burn 七数值列；BASE `boom01.ani` 前 4 帧有攻击框，SUB `boom02.ani` 未观察到盒字段。辅助对照同路径同形。 |
| `equipmentpassiveobject/requiem/003/boom2` | owner 写 `[basic action] Action/boom.act` 与 `[attack info] attackinfo/boom2.atk`，复用 requiem/003 目录 `boom.act/boom01.ani/boom02.ani`。`.atk` 为 `[damage bonus] 2900` 与 slow 五数值列；action 仍有攻击成功 appendage 行为，BASE `boom01.ani` 前 4 帧有攻击框，SUB `boom02.ani` 未观察到盒字段。辅助对照同路径同形。 |
| `actionobject/monster/timegate/conflagration/umtara/boom` | owner 写 `[attack info] AttackInfo/boom.atk` 和 `[basic action] Action/boom.act`，全 `passiveobject/` owner 搜索中该字符串仅命中 umtara。`.atk` 为 `[damage bonus] 600`、magic/no element/down，未观察到 `[active status]`；`.obj` 有 `[object destroy condition] [on end of animation]`。`boom.act [BASE ANI] -> boom_dodge.ani` 的 `FRAME000` 到 `FRAME007` 有 `[ATTACK BOX]`，`[SUB ANI] -> boom_normal.ani` 未观察到盒字段。辅助对照同路径同形。 |
| `actionobject/monster/3headlessknight -> 48163` | 父 action 创建 48163，回 passiveobject registry 命中 `Monster/HeadlessKnight/Horse_item.obj`；父 `empty_00.ani` 无盒，下游 `Horse.ani` 有 `[ATTACK BOX]`。 |
| `actionobject/monster/advancealtar/homingmoth` 与根级 `homingmoth` | 主目标 `passiveobject/actionobject/monster/` 下仅观察到 5 个 `homingmoth` 物理文件。全 PVF owner 搜索确认 `Action/HomingMoth.act` 与 `AttackInfo/HomingMoth.atk` 只由 `advancealtar/homingmoth.obj` 和根级 `homingmoth.obj` 两个 owner 引用；二者共用根级 `action/homingmoth.act`、`attackinfo/homingmoth.atk` 与 `animation/homingmoth.ani`。两个 owner 均有 `[homing follow] [ENEMY]`、`[homing velocity] 0 150`、`[homing check gap] 1000`、`[object destroy condition] [on end of animation]`；`.atk` 为 `[damage] 1600`、physic、no element、hit horizon、blow；`HomingMoth.ani` 17 帧均同时有 `[ATTACK BOX]` 与 `[DAMAGE BOX]`。`advancealtar/action/homingmoth.act` 在主目标未命中，不能按 owner 子目录猜物理 action。辅助对照同 5 文件和两个 owner 形态可读，主要差异为 name 字符串样式。 |
| `actionobject/monster/advancealtar/summonegg` 系列与根级 `summonegg` 系列 | 主目标 `summonegg` 相关物理文件共 24 个。`advancealtar/summonegg.obj` / `summonegg2-4.obj` 与根级同名对象共用根级 `Action/SummonEgg.act` / `SummonEgg2-4.act`、`Action/SummonEggBlast.act` / `SummonEggBlast2-4.act` 和 `Animation/SummonEgg.ani` / `SummonEgg2-4.ani`；`advancealtar/action/summonegg*.act` 不存在。四个 egg owner 均有 `[hp max]`、`[hp destroy] 1`、一个 basic action 和两个重复 `[last action]`：先 `Action/Mucus2.act`，再对应 `Action/SummonEggBlast*.act`。四个 `SummonEgg` action 均在 FRAME19 以 `[SUMMON MONSTER] [INDEX]` 召唤 253 / 61102 / 251 / 56101 并 `[DESTROY]`；这些数字按 `monster/monster.lst` 分别命中 `StarFishDevourer`、`BlackTentacle`、`StarFishKing`、`Stickcle`。其中 253 与 251 在 passiveobject registry 也撞号命中 swamp glass，对本上下文无效，不能按 passiveobject ID 解释。`SummonEgg*.ani` 20 帧中除 FRAME000/010 外均有 `[DAMAGE BOX] -26 -10 4 50 20 65`；`chargecircle.ani` 和 `SummonEggBlast.ani` 未观察到盒字段。辅助对照同 24 文件、同 summon ID 路由和核心列形可读，主要差异为 name 字符串样式及 registry 总量/行号。 |
| `actionobject/monster/advancealtar/mucus2` 与根级 `mucus2` | `Action/Mucus2.act` 只挂根级 `mucus2.ani`，该 ANI 未观察到盒字段；但 `advancealtar/mucus2.obj` 和根级 `mucus2.obj` 的 basic action 是 `Action/Mucus3.act`，并挂 `AttackInfo/Mucus2.atk`。`Mucus3.act` 指向根级 `Mucus3.ani`，FRAME000-004 有 `[ATTACK BOX]`，FRAME005 未观察到盒字段。`Mucus2.atk` 为 `[absolute damage] 0`、`[damage bonus] -70`、magic/no element/down，并有 `[active status] [poison] 40 0 20000 300` 四数值列。结论只属于实际 owner/action 链，不能从 `mucus2` 文件名推断 hitbox 来源。 |
| `actionobject/monster/advancealtar/icestalactite` 与根级 `icestalactite` | 主目标 `icestalactite` 相关物理文件共 6 个：根级 `action/icestalactite.act`、`action/icestalactitebroken.act`、`animation/icestalactite.ani`、`animation/icestalactitebroken.ani`、`advancealtar/icestalactite.obj` 与根级 `icestalactite.obj`。两个 owner 完全同形，均有空闭合 `[string data]`、`[etc action] Action/IcestalactiteBroken.act [/etc action]`、`[last action] Action/IcestalactiteBroken.act`、`[hp max] 80000`、`[hp destroy] 1`、`[team] 100`，但未观察到对象级 `[attack info]`。basic action 指向根级 `Icestalactite.ani`，broken action 指向根级 `IcestalactiteBroken.ani`；`Icestalactite.ani` FRAME000 同时有 `[ATTACK BOX]` 与 `[DAMAGE BOX]`，FRAME001 只有 `[DAMAGE BOX]`；`IcestalactiteBroken.ani` 四帧未观察到盒字段。辅助对照同 6 文件、同 action/ANI 核心列形可读，主要差异为主目标 name 是字符串链接样，辅助为直接文本。 |
| `actionobject/monster/advancealtar/rocket` / `rocketblast` 与根级同名对象 | 主目标 `advancealtar/rocket.obj` 与根级 `rocket.obj` 同形：width `10 5`、floating height `1`、pass all、piercing power `0`、`[basic action] Action/Rocket.act`、`[attack info] AttackInfo/Rocket.atk`、空闭合 `[etc action]`、`[last action] Action/RocketBlast.act`、`[hp max] 8000`、`[hp destroy] 1`。`Action/Rocket.act` 只由这两个 rocket owner 引用，物理 action 在根级 `action/rocket.act`，`advancealtar/action/rocket*.act` 未观察到。`Rocket.act` 指向 `Rocket.ani`，两帧均有 `[ATTACK BOX]`。`AttackInfo/Rocket.atk` 在主目标被 6 个 owner 复用：advancealtar/root 的 `book1.obj`、`miatalonsmall.obj`、`rocket.obj`；因此不能写成 rocket 专属 `.atk`。`advancealtar/rocketblast.obj` 与根级 `rocketblast.obj` 同形，挂 `Action/RocketBlast.act` 与 `AttackInfo/RocketBlast.atk`，team `100`；`RocketBlast.act` 指向 `RocketBlast1.ani` 和 `RocketBlast2.ani`，二者均有攻击盒，并在 FRAME6 执行 `[DESTROY]`、ON SET ACTION 时 `[SET TEAM] 50`。同字符串 `Action/RocketBlast.act` 在 `roket/rocketblast.obj` 等其他 owner 中也出现，但 `roket` 目录解析到独立物理 `.act/.atk`，其 `.atk` 另有 `[attack friend] 1`，不能与根级合并。辅助对照同路径核心结构和 ANI 盒字段同形；辅助对照额外观察到 `drop_rocket.obj` 复用 `AttackInfo/Rocket.atk`，只能作为辅助差异提示。 |
| `actionobject/monster/advancealtar/rocketman_*` 与根级同名对象 | 主目标 `advancealtar` 下观察到 7 个 `rocketman` 对象：`bullet`、`missile`、`rocket1`、`rocket11`、`rocket2`、`rocket22`、`warningmark`；根级同名 7 个对象同形。额外根级 `rocketman_missile2.obj` 复用 `Action/RocketMan_Missile.act` 但挂 `AttackInfo/RocketMan_Missile2.atk`，额外根级 `rocketman_missile_item.obj` 走 `Action/rocket_item.act` 并挂 `AttackInfo/RocketMan_Missile.atk`。`missile.obj` 有闭合 `[int data] 2000 5000 [/int data]`；`warningmark.obj` 不挂 `.atk`，有 `[homing]` 块：use `1`、follow `[ENEMY]`、velocity `600 5`、check gap `200`。`rocket1.act` 创建 8174，命中 `ActionObject/Monster/RocketMan_Rocket2.obj`；`rocket11.act` 在 `[WHICH] [MONSTER] [IS INDEX] 61235` 检查后创建 10281，61235 走 `monster/monster.lst` 命中 AT-5T，10281 走 passiveobject registry 命中 `RocketMan_Rocket22.obj`；`rocket2.act` / `rocket22.act` 创建 8030，命中 `RocketBlast.obj`；`rocket_item.act` 四次创建 48143，命中 `RocketMan_Missile2.obj`。`RocketMan_Bullet.ani` 4 帧均有攻击盒；`RocketMan_Missile.ani`、`RocketMan_Rocket1.ani`、`RocketMan_Rocket2.ani` 每帧都有 attack/damage box；`RocketManWarningMark.ani` 与 `rocket_item.ani` 未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`。辅助对照同路径核心对象、action、registry 路由和 ANI 盒字段同形，主要差异为 name 字符串样式、registry 总量/行号，以及 `Action/RocketManWarningMark.act` owner 搜索在辅助对照中命中 10 个对象而主目标为 3 个。 |
| `actionobject/monster/advancealtar/arrow_lv1-4` | 主目标 `advancealtar/arrow_lv1.obj` 到 `arrow_lv4.obj` 四个对象同构：width `5 5`、floating height `1`、normal layer、pass all、team `100`，均挂 `Action/Arrow.act`，分别挂 `AttackInfo/Arrow_Lv1.atk` 到 `AttackInfo/Arrow_Lv4.atk`。共享 `Arrow.act` 只含 MOTION BASE `../Animation/Arrow.ani` 与 sound `SCARECROW_SPINATK 0`，未观察到 `[CREATE PASSIVEOBJECT]`、`[SUMMON MONSTER]`、`[ATTACKRECT]` 或 action 级销毁行为。四个 `.atk` 结构同形，`[damage]` 分别为 `10000 / 13000 / 17000 / 25000`，其余核心列形为 weapon damage apply `1`、physic、attack enemy `1`、no element、damage reaction damage、push aside `100`、lift up `300`、hit wav `A_WHEEL_HIT`、hit down、blow、no blood `200 1.0`、max target hit number `1`。`Arrow.ani` 为 LOOP 1 / FRAME MAX 4，四帧均有 `[ATTACK BOX]`：FRAME000/002 为 `-48 -10 -20 102 20 40`，FRAME001/003 为 `-48 -16 -21 102 31 41`，未观察到 `[DAMAGE BOX]`。主目标 `Action/Arrow.act` 字符串还命中 `act8/monster/arrow.obj`，且 `../Animation/Arrow.ani` 也被 act8 多个 action 复用；结论必须限定到 advancealtar owner。辅助对照同路径对象、action、atk、ANI 核心列形同形；辅助对照额外有 `advanceatlar` 拼写路径和更多 arrow owner，主目标未观察到，不能写成主目标存在。 |
| `actionobject/monster/advancealtar/bomb_lv1-4 -> LgExp_Lv1-4` | 主目标 `bomb_Lv1-4` 在 `passiveobject/passiveobject.lst` 中分别为 12601 / 12643 / 12644 / 12645；四个 `.obj` 同构：width `5 5`、floating height `1`、normal layer、pass all、piercing power `1000`、team `100`，共享 `Action/bomb.act`，分别挂 `Action/bomb1_Lv1-4.act` 与 `AttackInfo/bomb_Lv1-4.atk`。`bomb.act` 指向 `bomb.ani`，在 `ZPOS < 10` 时 `[SET ACTION] [CUSTOM] 0 [NOW]`；`bomb1_Lv1-4.act` 均在 FRAME6 创建 passiveobject，ID 12600 / 12646 / 12647 / 12648 回 `passiveobject/passiveobject.lst` 命中 `LgExp_Lv1-4.obj`，创建块为 empty particle、level `0`、pos `0 0 -30`，随后 `[DESTROY]`。`LgExp_Lv1-4.obj` 同构，挂 `Action/LgExp.act`、`attackinfo/FireExplosion_Lv1-4.atk`，并有 `[object destroy condition] [on end of animation]`；`LgExp.act` 指向 `FireExplosion.ani`，FRAME0/1/2 分别播放四个 `FireExplosionParticle*.ptl` 后在第三个行为块 `[DESTROY]`。父层 `.atk` 伤害为 `2000 / 3000 / 4000 / 5000`，下游爆炸 `.atk` 伤害为 `4000 / 6000 / 9000 / 19000`；二者都是 physic/fire/blow 类 payload，盒坐标仍来自 ANI。`bomb.ani` 四帧均有 `[ATTACK BOX] -11 -10 -9 21 20 21`；`bomb1.ani` 七帧均有 `[ATTACK BOX] -11 -10 -10 20 20 22`；`FireExplosion.ani` 三帧均有 `[ATTACK BOX] -65 -40 -50 130 80 100`，三者均未观察到 `[DAMAGE BOX]`。主目标 owner 搜索显示 `Action/bomb.act` 字符串还命中 monster `.mob` 与其他 passiveobject owner，`Action/LgExp.act` 还被 crokhan 与 `lgexp2_lv*` 等 owner 复用，结论必须限定到本 owner 链。辅助对照核心链路、ID 路由和 ANI 盒字段同形；差异为父对象 name 文本样式、registry 总量和行号。 |
| `actionobject/monster/advancealtar/catpul_bomb* -> catpul_BombSub* -> OilExp` | 主目标该前缀下 `catpul_bomb` 文件清单命中 24 个文件：11 个 action、5 个 attackinfo、8 个 `.obj`。`passiveobject/passiveobject.lst` 中 12621 / 12623 / 12625 / 12627 分别命中 `catpul_bomb.obj` 到 `catpul_bomb3.obj`，12622 / 12624 / 12626 / 12628 命中 `catpul_BombSub.obj` 到 `catpul_BombSub3.obj`，12634 命中 `OilExp.obj`；raw path 含 `Monster/AdvanceAltar` 仍按 passiveobject registry 解析。四个父弹对象同构：width `5 5`、floating height `1`、normal layer、pass all、piercing power `1000`、team `100`，共享或分级挂 `Action/catpul_bomb*.act` / `Action/*catpul_bomb1.act` 与 `AttackInfo/catpul_bomb.atk`。父弹飞行动作在 `ZPOS < 10` 时切 custom action；落地 action 分别在 FRAME6 或 FRAME3 创建 12622 / 12624 / 12626 / 12628，创建块为 empty particle、level `0`、pos `0 0 -10`，随后 `[DESTROY]`。四个 sub 对象挂 `catpul_bomb_exp.atk`、`1catpul_bomb_exp.atk`、`2catpul_bomb_exp.atk`、`3catpul_bomb_exp.atk`，伤害为 `8000 / 10000 / 19000 / 29000`；前三层常规 sub action FRAME10 销毁，`3catpul_bombsub.act` 额外在 FRAME4 创建 12634 `OilExp.obj` 后 FRAME10 销毁。`OilExp.obj` 挂 `Action/OilExp.act` 与 `AttackInfo/catpulStinger.atk`；`OilExp.act` 在 FRAME1/3/5/7/9/11 执行 `[ATTACKRECT] [RESET]`，FRAME13 `[DESTROY]`。父 `.atk` 伤害 `5000`；sub `.atk` 均为 physic/fire/blow 类 payload，其中 2/3 级为 down / hit down；`catpulStinger.atk` 伤害 `4500`、physic/fire、none reaction、push aside `0`、hit wav `FIRE_PILLAR_HIT`。ANI 层中 `BombFly`、`CannonBall(_boom)`、`Fireball(_eff)`、`bomb1`、`catpul_EarthQuake(_S)` 和 `ExpFloorNormal` 均有 `[ATTACK BOX]` 正样本，ring/flash/charge/dust/dodge/normal 表现 SUB ANI 未观察到盒字段；未观察到 `[DAMAGE BOX]`。`Action/OilExp.act` 在主目标被 6 个 owner 复用，`Action/catpul_bomb.act` 命中两个父弹 owner，其余本桶 action/atk 多为当前 owner 单命中；结论必须限定到当前 owner 链。辅助对照同样观察到 24 个 `catpul_bomb` 文件和核心 `3catpul_bombsub.act -> 12634` 链路；差异为 name 文本样式、registry 总量和行号，不能覆盖主目标。 |
| `actionobject/monster/advancealtar/probomb_lv1-4 -> LgExp2_Lv1-4` | 主目标 `probomb` 文件清单命中 16 个文件：8 个 action、4 个 ANI、4 个 `.obj`；另需继续闭合创建出的 `LgExp2_Lv1-4.obj`、共用 `Action/LgExp.act`、`FireExplosion.ani` 和 `FireExplosion*.atk`。`passiveobject/passiveobject.lst` 中 12615 / 12674 / 12675 / 12676 分别命中 `ProBomb_Lv1-4.obj`，12616 / 12677 / 12678 / 12679 分别命中 `LgExp2_Lv1-4.obj`，raw path 含 `Monster/AdvanceAltar` 仍按 passiveobject registry 解析。四个父对象同构为 normal layer、pass all、piercing power `1000`、basic action 和 name；父对象不挂 `[attack info]`。`ProBomb_Lv1.act`、`2ProBomb.act`、`3ProBomb.act`、`4ProBomb.act` 均在 `ZPOS <= 10` 且 limit `1` 后创建下游 passiveobject，ID 分别为 12616 / 12677 / 12678 / 12679，创建块为空 particle、level `0`、pos `0 0 0`，随后 `[DESTROY]`；其中 `4ProBomb.act` 实际 BASE ANI 为 `../Animation/3ProBomb.ani`，不是 `4ProBomb.ani`。同名字符串 `Action/ProBomb.act` 命中 `blood_an/probomb.obj`，按 owner 目录解析不属于本 advancealtar 链；`ProBomb_Lv2-4.act` 物理存在但主目标脚本文本 owner 搜索未命中。`LgExp2_Lv1-4.obj` 共用 `Action/LgExp.act`，有 `[object destroy condition] [on end of animation]`，分别挂 `attackinfo/FireExplosion.atk`、`FireExplosion1.atk`、`FireExplosion2.atk`、`FireExplosion3.atk`。下游 `.atk` 伤害为 `13000 / 16000 / 20000 / 30000`，核心 payload 为 weapon damage apply `1`、physic、attack enemy `1`、fire element、down、push aside `150`、lift up `400`、knuck back `1`、hit wav `FIRE_EXPLOSION_CREATE`、blow、no blood `100 1.0`。`ProBomb.ani`、`2ProBomb.ani`、`3ProBomb.ani`、`4ProBomb.ani` 均为单帧图像样本，未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`；当前 owner 链直接链接前三者，其中 4 级复用 `3ProBomb.ani`。`FireExplosion.ani` FRAME000-002 均有 `[ATTACK BOX] -65 -40 -50 130 80 100`，未观察到 `[DAMAGE BOX]`。`Action/LgExp.act` 在主目标被 9 个 owner 复用，`FireExplosion.atk` 被 crokhan 和 `lgexp2_lv1` 两个 owner 复用，`FireExplosion1/2/3.atk` 各只命中对应 `lgexp2_lv2-4`；结论必须限定到本 ProBomb -> LgExp2 链。辅助对照同样观察到 16 个 `probomb` 文件，Lv1 action、12616/12677/12679 registry 路由、`ProBomb.ani` 无盒与 `FireExplosion.ani` 攻击盒同形；差异为 name 文本样式、registry 总量和行号。 |
| `actionobject/monster/advancealtar/zepplinbullet_lv1-4` | 主目标 `zepplinbullet` 文件名命中 7 个文件，本桶只覆盖 `passiveobject/actionobject/monster/advancealtar/zepplinbullet_lv1-4.obj`；`passiveobject/actionobject/monster/riding/zepplinbulletexp.obj` 与 `passiveobject/monster/zepplin/zepplinbullet*.obj` 是相邻边界，不并入本结论。`passiveobject/passiveobject.lst` 中 12614 / 12680 / 12672 / 12673 分别命中 `ActionObject/Monster/AdvanceAltar/ZepplinBullet_Lv1-4.obj`，raw path 含 `Monster/AdvanceAltar` 仍按 passiveobject registry 解析。四个对象同构为 width `1 1`、floating height `1`、normal layer、pass all、piercing power `0`、basic motion `animation/Bullet.ani`，分别挂 `AttackInfo/Bullet_Lv1.atk` 到 `Bullet_Lv4.atk`。四个 `.atk` 同构：damage bonus `-80`、attack type `ignore defense`、weapon damage apply `1`、attack enemy `1`、no element、damage reaction `damage`、push aside `100`、lift up `50`、hit info `blow`、no blood `0 1.0`、hit wav `SLUG_HIT`。`animation/Bullet.ani` 为 5 帧，每帧均有 `[ATTACK BOX]`，坐标依次为 `-7 -7 -6 12 14 14`、`-7 -7 -6 13 14 14`、`-8 -8 -7 13 15 15`、`-8 -8 -7 15 15 15`、`-8 -7 -6 14 15 14`；未观察到 `[DAMAGE BOX]`。主目标 `monster/newmonsters/advancealtar/tower/goblin_propeller/action/lv1-4_shootup_stay*.act` 共 8 个 action 均创建 12614 Lv1 子弹，创建块使用 `../Particle/Bullet.ptl`、level `0`、pos `90 0 -50` 与 `100 -10 -50`、`[USE MY DIRECTION]`、`[FIX DIRECTION]`、`[NEUTRAL]`；该 particle 为 straight line、move variable `600 600`、rotation angle `0 -45 0`、life time `2000`、landing type `vanish`。在同一 `monster/newmonsters/advancealtar` 子树下未观察到 12680 / 12672 / 12673 的创建入口，Lv2-4 只能写成 registry 与对象可解析，不能写成当前主目标已有创建链。辅助对照同形观察到 Lv1 对象、atk、ANI、12614 创建入口和 12680/12672/12673 registry；差异为辅助对照额外存在 `advanceatlar` 拼写路径、name 文本样式、registry 总量和行号，不能覆盖主目标。 |
| `actionobject/monster/advancealtar/denture3_lv1-4` | 主目标 `denture3` 文件名命中 7 个文件，本桶只覆盖 `passiveobject/actionobject/monster/advancealtar/denture3_lv1-4.obj` 与同目录 `action/denture3.act`；`passiveobject/actionobject/act8/monster/crokhan/denture3.obj` / `action/denture3.act` 是同名相邻边界，不并入本结论。`passiveobject/passiveobject.lst` 中 12593 / 12668 / 12669 / 12670 分别命中 `ActionObject/Monster/AdvanceAltar/denture3_Lv1-4.obj`，raw path 含 `Monster/AdvanceAltar` 仍按 passiveobject registry 解析。四个对象同构为 normal layer、pass all、piercing power `1000`、basic action `Action/denture3.act`、`[object destroy condition] [destroy condition] [time limite] 20000`，分别挂 `attackinfo/denture2_Lv1.atk` 到 `denture2_Lv4.atk`；四个 `.atk` owner 搜索各只命中对应 `denture3_lv*` 对象。`denture2_Lv1-4.atk` 同构，伤害为 `15000 / 20000 / 26000 / 40000`，核心 payload 为 physic、weapon damage apply `1`、attack enemy `1`、no element、damage reaction damage、hit wav `1ST_STRONG_LOWKICK_HIT`、hit horizon、blow、no blood `1 1.2000000476837158`、active status slow `100 70 2500 25 25`。`action/denture3.act` BASE ANI 为 `../animation/denture2.ani`，FRAME0 播放 `[PARTICLE] ../particle/teeth.ptl 0 0 0`，未观察到 `[CREATE PASSIVEOBJECT]` 或显式 `[DESTROY]`。`denture2.ani` 为 2 帧，两帧均有 `[ATTACK BOX] -68 -32 -132 119 60 272`，未观察到 `[DAMAGE BOX]`；`teeth.ptl` 是粒子侧车，引用 `teeth1.ani` 到 `teeth4.ani`，四个 teeth ANI 均为显示帧，未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`。主目标创建入口为 `monster/newmonsters/advancealtar/friend/knifetooth/action/bite2_lv1-4.act`，分别创建 12593 / 12668 / 12669 / 12670，创建块使用 `../Particle/denture.ptl`、level `0`、pos `90 0 80`；该 source particle 为 straight line，x move variable `450 450`、z move variable `50 10`、gravity `1000`、landing type vanish、life time `30000`。辅助对照同形观察到正确拼写 `advancealtar` 对象、action、Lv1/Lv4 atk、`denture2.ani`、registry 和 `bite2_lv1` 创建入口；差异为辅助对照额外存在 `advanceatlar` 拼写路径、`95object/map/harlem` action 路径、更多数字碰撞和不同 registry 总量/行号，其中辅助 `advanceatlar/bite2_lv1.act` casting 为 `900`，不能覆盖主目标 casting `300`。 |
| `actionobject/monster/advancealtar/iceniddle` | 主目标 `iceniddle` 全局文件名命中 58 个文件，本桶只覆盖 `passiveobject/actionobject/monster/advancealtar/iceniddle.obj`、`action/iceniddle.act`、`attackinfo/iceniddle.atk`、`animation/iceniddle.ani` 和 `animation/iceniddle_glow.ani`；`common`、`flydragon`、`soceress` 和辅助对照 `advanceatlar` 拼写分支是相邻边界。`passiveobject/passiveobject.lst` 中 12606 命中 `ActionObject/Monster/AdvanceAltar/Iceniddle.obj`，raw path 含 `Monster/AdvanceAltar` 仍按 passiveobject registry 解析。对象为 width `5 5`、normal layer、pass all、piercing power `1000`、team `100`，挂 `Action/Iceniddle.act` 与 `AttackInfo/Iceniddle.atk`；`name` 在主目标为 `<9::Turtlek_02\`胆小的海龟\`>` 字符串链接样，不能据此解释为对象语义。`Action/Iceniddle.act` BASE `../animation/iceniddle.ani`，SUB `../animation/iceniddle_glow.ani 0 1`；FRAME3/5/8/10 均触发同一个 `[ATTACKRECT] [RESET]` 行为，FRAME14 `[DESTROY]`，未观察到下游 `[CREATE PASSIVEOBJECT]`。`AttackInfo/Iceniddle.atk` 伤害 `3000`，payload 为 physic、attack enemy `1`、water element、damage reaction damage、hit wav `ICICLE_HIT`、hit down、blow、no blood `50 1.0`。`iceniddle.ani` 为 15 帧，FRAME002-010 有 `[ATTACK BOX]`，多帧含多条攻击盒，未观察到 `[DAMAGE BOX]`；`iceniddle_glow.ani` 为 11 帧显示层，未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`。主目标 `Action/Iceniddle.act` 与 `AttackInfo/Iceniddle.atk` owner 搜索各只命中当前对象；12606 创建入口为 `monster/newmonsters/advancealtar/enemy/keraha/action/cast.act` / `cast1.act` 与 `.../vinoshu/action/cast.act` / `cast1.act`，其中 `cast.act` 在 FRAME8 目标检查后创建 12606，empty particle、level `0`、pos `0 0 0`、warning mark `0 0 100 1`；`cast1.act` 在 FRAME6/7/8/8 创建 4 次，pos `100/200/300/400 0 0`，同样使用 warning mark。辅助对照同形观察到正确拼写对象、action、atk、ANI、12606 registry 和 keraha/vinoshu 创建入口；差异为辅助对照额外存在 `advanceatlar` 拼写对象/action/atk/ANI/创建入口，且 name 为直接文本 `标枪`，不能覆盖主目标。 |
| `actionobject/monster/at_5t_walker/rocket1 -> rocket2 -> rocketboom` | 主目标该前缀下文件清单为 92 个文件，当前只闭合 rocket 子链。`rocket1.obj` 挂 `Action/rocket1.act` 与 `AttackInfo/rocket1.atk`；`rocket1.act` 在 `ZPOS >= 350` 后三次创建 10263 并销毁，10263 走 `passiveobject/passiveobject.lst` 命中 `ActionObject/Monster/at_5t_walker/rocket2.obj`。`rocket2.obj` 挂 `Action/rocket2.act` 与 `AttackInfo/rocket2.atk`；`rocket2.act` 在 `ZPOS <= 20` 后创建 10267 并销毁，10267 命中 `ActionObject/Monster/at_5t_walker/rocketboom.obj`；同 action 还在攻击成功后检查 61236，61236 走 `monster/monster.lst` 命中 `NewMonsters/Cartel/warjack/warjack.mob`。`rocketboom.obj` 与物理存在的 `rocketboom1.obj` 共用 `Action/rocketboom.act`，但分别挂 `JeffMissileEXP.atk` / `JeffMissileEXP1.atk`；`rocketboom.act` 指向 `JeffMissileEXP.ani` 及 3 个 SUB ANI，攻击成功检查 61236 后扣血，ON SET ACTION 设置 team `0`，FRAME5 销毁。`rocket1.ani` 未观察到盒字段；`rocket2.ani` 有一条 `[ATTACK BOX]`；`JeffMissileEXP.ani` FRAME000-003 有多条 `[ATTACK BOX]`，`JeffMissileEXP1/2/3.ani` 未观察到盒字段。`Action/rocket1.act`、`Action/rocket2.act`、`Action/rocketboom.act` 和 `AttackInfo/JeffMissileEXP*.atk` 均存在跨 owner 字符串复用，必须按 `at_5t_walker` owner 目录解析物理文件。辅助对照同路径核心结构、registry 路由和 ANI 盒字段同形，主要差异为 name 字符串样式、registry 总量/行号。 |
| `actionobject/monster/at_5t_walker/fire-bullet-bombingmissile-exp` | 主目标该前缀下文件清单为 92 个文件，当前只闭合 fire / bullet / bullet1 / bombingmissile / exp / hit 这一组，不声明全前缀完成。`fire.obj`、`bullet.obj`、`bullet1.obj`、`bombingmissile.obj`、`exp.obj` 均闭合到各自 basic action 与 attackinfo；`bullet.obj` 另有 `Action/hit.act` etc action。`fire.act` 多帧执行 `[ATTACKRECT] [RESET]`，并在攻击成功后检查 61236；`Bullet.act` / `Bullet1.act` 在攻击成功后检查 61236，`[CHECKUP OBJECT] [RANDOM SELECT]` 下的 0-9 / 1-9 是检查对象候选，不按 registry ID 解释；`BombingMissile.act` 在 `ZPOS <= 30` 后创建 10284，10284 回 `passiveobject/passiveobject.lst` 命中 `ActionObject/Monster/at_5t_walker/rocketboom1.obj`；`Exp.act` 在 FRAME1 创建 8715，8715 回 passiveobject registry 命中 `ActionObject/Monster/Giselle/GiselleFloor.obj`；61236 回 `monster/monster.lst` 命中 `NewMonsters/Cartel/warjack/warjack.mob`。ANI 层：`fire.ani` FRAME000-016 有攻击框、FRAME017-020 无盒；`Bullet.ani` 三帧均有攻击框；`Bullet1.ani`、`hit.ani` 无盒；`BombingMissile.ani` 单帧有攻击框；`ATExpNormal.ani` FRAME000-005 有攻击框、FRAME006-007 无盒；`ExpNormal.ani` 与 `ATExpDodge.ani` 无盒；8715 下游 `ExpFloorNormal.ani` FRAME001-002 有攻击框，`ExpFloorDodge.ani` 无盒。`Action/BombingMissile.act`、`Action/Exp.act` 与 10284/8715 入边均观察到跨 owner 复用，结论只属于本 owner 链。辅助对照同路径核心结构、registry 路由和 ANI 盒字段同形；主要差异为 `fire.obj` 的 sound category、name 字符串样式、registry 总量/行号。 |
| `actionobject/monster/at_5t_walker/special -> 10268 bombingmissile` | 主目标 `special.obj` 挂 `Action/special.act`，`[attack info]` 为空字符串；无对象级销毁块。`special.act` BASE `special.ani`，SUB `special1.ani` / `special2.ani`，有 `AIRPLANE_02` 声音；ON SET ACTION 后设置移动方向、X 轴速度 `800`，记录 `time = GET TIME` 并打开 trigger 1；当 `time + 2000 <= GET TIME` 后执行 16 个 `[CREATE PASSIVEOBJECT] [INDEX] 10268` 块，均为 level `0`、空粒子、`[USE MAP POS]` 和 `[WARNING MARK] 0 0`，位置由三组 `[RANDOM]` 列控制，随后关闭 trigger 1 并 `[DESTROY]`。10268 回 `passiveobject/passiveobject.lst` 命中同前缀 `BombingMissile.obj`；`BombingMissile.act` 后续仍可创建 10284 `rocketboom1.obj` 并在攻击成功后检查 61236。`special.ani`、`special1.ani`、`special2.ani` 均为单帧 MechaDropShadow 图像，未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`；本桶直接 hitbox 来自下游 10268 的 `BombingMissile.ani`，而不是 special 自身 ANI。主目标 `Action/special.act` 字符串被 racing、at_5t_walker 与 equipmentpassiveobject owner 复用，必须按 owner 目录解析；主目标 10268 SearchNum 只命中 at_5t_walker `special.act` 与 registry。辅助对照同路径核心结构、10268 路由和 special ANI 无盒结论同形；差异为 registry 总量/行号和 name 文本细节。 |
| `actionobject/monster/at_5t_walker/barr-destroy-smoke` | 主目标当前闭合 `barr.obj`、`barr1.obj`、`barr2.obj`、`destroy.obj`、`destroydust.obj`、`shootsmoke.obj`。三个 `barr*` 对象均为 `[pass type] [do not pass]`、`[is blocking obj] 1`、`[hp max] 100000`、`[hp destroy] 1`、`[attack info]` 为空字符串、`[destroy particle] Particle/barrdestroy.ptl`，并有 `[can beat index] 61235 [/can beat index]`；61235 回 `monster/monster.lst` 命中 `NewMonsters/Riding/at_5t/at_5t.mob`，主目标 passiveobject registry 未命中。三者以 `[basic motion]` 直连 `barr.ani` / `barr1.ani` / `barr2.ani`，三个 ANI 均为单帧 `[DAMAGE BOX]` 正样本，未观察到 `[ATTACK BOX]`。`destroy.obj` 挂 `Action/destroy.act`，action FRAME0 只播放 `../particle/destroy.ptl`；`destroy.ani` 是空图无盒。`destroydust.obj` 与 `shootsmoke.obj` 均为 basic motion 直连表现 ANI，`.atk` 为空；`destroydust.ani` 22 帧尘土表现无盒，`shootsmoke.ani` 6 帧枪烟表现无盒。`barrdestroy.ptl` 和 `destroy.ptl` 只登记为 `.ptl` 侧车资源引用，不证明客户端资源完整。辅助对照同路径对象、ANI、61235 registry 路由和粒子核心列形同形，主要差异为 registry 总量/行号与 name 文本细节。 |
| `actionobject/common/` 文件清单 | 主目标该前缀下观察到 772 个文件；这是 actionobject/common，不等同于 `passiveobject/common/`。 |
| `actionobject/common/bigboom.obj` | `.obj` 同时挂 `BigBoom.act` 与 `BigBoom.atk`；BASE `Bigboom1.ani` 有 `[ATTACK BOX]`，四个 SUB ANI 未观察到盒字段；`[ATTACKRECT] [RESET]` 只登记为 action 行为字段。 |
| `actionobject/SPC/` 文件清单 | 主目标该前缀下观察到 1259 个文件；SPC 可含恢复/扣血、陷阱、发射物和跨 common 下游链。 |
| `actionobject/SPC/4season` | `hp_create` 随机创建 11008/11009，分别回 registry 命中 `fairy_ok/fairy_bad`；`fairy.ani` 每帧有 `[ATTACK BOX]`，恢复/扣血行为在 action 的 `[RESTORE] [HP] +/-10 [%]` 中。 |
| `actionobject/SPC/_cannontrap -> _cannonball -> SmallBoom3` | trap 本体 `stay.ani` / `attack.ani` 有 `[DAMAGE BOX]`；8044 发射物 `body.ani` 有 `[ATTACK BOX]`；8846 下游 common `SmallBoom3` 的 `NapalmBombExplosion.ani` 有 `[ATTACK BOX]`。 |
| `actionobject/SPC/jackie` | `frenken_ball` / `frenken_ball_count` / `frenken_ball_count_bomb` 形成球体、倒数、爆发链；16070/16071 回同子桶，9950 跨到 `ActionObject/Monster` 前缀，8580 跨到 `ActionObject/Common`；本体部分 ANI 有 `[DAMAGE BOX]`，8580 下游 common BASE ANI 有 `[ATTACK BOX]`。 |
| `actionobject/monster/cartelcommand/robot` | 9950 已回 `passiveobject/passiveobject.lst` 命中 `ActionObject/Monster/cartelcommand/robot.obj`；该对象只有 `[basic action] Action/robot.act`，不挂 `.atk`。`robot.act` 指向 BASE `robot.ani` 与 SUB `die_eff2.ani`，两个 ANI 均未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`。主目标另有 `ActionObject/Map/Risk_Dungeon/cartelcommand/robot.obj` 使用同一相对 action 字符串，必须按 owner 目录区分。辅助对照同 9950 链可读，但额外观察到 `q_cartelcommand/robot.obj` owner，不能写成主目标存在。 |
| `actionobject/SPC/despair_tower/sjar` | coin ok/bad/summon 分支同时覆盖 passiveobject 创建和 monster 召唤：9202/8376/8375/9203/9204/9206 走 passiveobject registry，召唤候选走 `monster/monster.lst`；中间多层 ANI 多为无盒表现层，末层 `JeffMissileEXP.ani` 有 `[ATTACK BOX]`。 |
| `actionobject/SPC/despair_tower/sjar ok -> ZDrop -> ZGT -> ZMissile -> ZMissileExp` | 主目标当前只闭合 `sjar_coin_bom` 的 ok 分支，不并入 fortune/summon/bad 分支。`sjar_coin_bom.obj` 为 normal layer、pass all、piercing power `1000`、team `100`，basic action 为 `sjar_coin_bom_appear.act`，etc action 为 `sjar_bom_ok.act` / `sjar_bom_bad.act`，对象本体未观察到 `[attack info]`。`sjar_bom_ok.act` BASE 为 `sjar_coin_ok.ani`，FRAME1 限次触发 `[RANDOM SELECT] 0 1` 后创建 9202，两种行为均为空 particle、`[USE MAP POS]` 和固定方向，位置分别为 `159 224 0` 右向 level `70`、`923 254 0` 左向 level `0`，FRAME2 自毁。9202 / 9203 / 9204 / 9206 均经 `passiveobject/passiveobject.lst` 分别命中 `ZDrop.obj`、`ZGT.obj`、`ZMissile.obj`、`ZMissileExp.obj`。`ZDrop.obj` 不挂 `.atk`，三份 action 均创建 9203；`ZGT.act` FRAME6 创建 9204、FRAME9 自毁；`ZMissile.obj` 实际 basic action 是 `Action/Missile.act`，不是 `ZMissile.act`，对象有 on-end-animation 销毁，`Missile.act` 在 `ZPOS <= 40` 时创建 9206 并播放粒子/震屏/自毁，攻击成功也执行自毁行为，FRAME0 设置 Z 轴速度 `-430` 和 team `50`。`ZMissileExp.obj` 挂 `Action/JeffMissileEXP.act` 与 `AttackInfo/JeffMissileEXP.atk`，time limit `1000`；`.atk` 为 absolute damage `35000`、weapon damage apply `1`、physic/fire/down、attack enemy `1`、push/lift `50/500`、hit lift up、blow、no blood `100 1.0`。中间 `MechaDrop*`、`dumy2`、`WarningMark`、`Missile.ani` 未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`；末层 `JeffMissileEXP.ani` FRAME000 有 2 条攻击盒，FRAME001-003 各 3 条攻击盒，FRAME004-005 无盒，三个 SUB ANI `JeffMissileEXP1/2/3.ani` 未观察到盒字段。本 13 个源文件内 `[CREATE PASSIVEOBJECT]` 只命中 ok、ZDrop 三 action、ZGT 与 Missile；未观察到 `[SUMMON MONSTER]`、`[homing]` 或 `[active status]`。辅助对照同 19 个 sjar 文件、同 9202/9203/9204/9206 registry 路由、核心 action/atk/ANI 盒字段同形；差异为对象 `[name]` 文本样式、registry 总量/行号和部分对象脚本长度。 |
| `actionobject/SPC/despair_tower/sjar bad -> MinePressure2 -> MineExplosion2` | 主目标当前闭合 `sjar_coin_bom` 的 bad 分支到 trap/common 下游，不并入 ok、fortune 或 summon 分支。`sjar_bom_bad.act` BASE 为 `sjar_coin_bad.ani`，FRAME1 限次触发 `[RANDOM SELECT] 0 1` 后选择创建 8376：第一行为含 3 个创建块且 level 均为 `70`，第二行为含 5 个创建块且首块 level `0`、其余 level `70`；每个创建块均为空 particle，`[POS]` 下有 `[RANDOM] 20 300` 与 `[RANDOM] 0 70 -120`，并使用 `[USE MY BASEPOS]`、`[USE OBJECT ZPOS]`、`[USE MY DIRECTION]`，FRAME2 自毁。8376 经 `passiveobject/passiveobject.lst` 命中 `ActionObject/Trap/MinePressure2.obj`；对象为 bottom layer、pass all、piercing power `1000`，basic action `Action/MineStay2.act`，etc action `Action/MineActive2.act`，attack info `AttackInfo/MinePressure.atk`，destroy particle `MinePressureDestroy.ptl`。`MineStay2.act` BASE `MinePressureReady2.ani`，SUB `MineAlram2.ani 0 0`，角色距离 `< 100` 时切到 custom action 0；`MineActive2.act` BASE `MinePressureActive2.ani`，SUB `MineAlram2.ani 0 -1` 与 `MinePressureReady2.ani 0 -2`，FRAME1 创建 8375，level `60`、pos `0 0 0`，随后自毁。8375 经同一 registry 命中 `ActionObject/Common/MineExplosion2.obj`；对象为 normal layer、pass all、piercing power `1000`，basic action `Action/MineExplosion.act`，attack info `AttackInfo/MineExplosion2.atk`。`MinePressure.atk` 只有 `[damage reaction] [none]`；`MineExplosion2.atk` 为 damage `1000`、magic、attack enemy `1`、fire element、damage reaction damage、push aside `50`、lift up `100`、hit wav `FIRE_EXPLOSION_CREATE`、blow、no blood `100 1.0`，`.atk` 均不提供 hitbox 坐标。`MineAlram2.ani` FRAME000/001 均有 `[ATTACK BOX] -16 -10 -6 31 20 20`；`MinePressureReady2.ani`、`MinePressureActive2.ani` 未观察到盒字段。`MineExplosion.act` BASE `FireExplosion.ani`，SUB `FireExplosionParticle1/2/3/4.ani`；`FireExplosion.ani` FRAME000 有 `[ATTACK BOX] -65 -40 -50 130 80 100`，FRAME001 无盒，四个粒子 SUB ANI 未观察到盒字段。限定检索中，本桶脚本 `[CREATE PASSIVEOBJECT]` 只命中 `sjar_bom_bad.act` 和 `MineActive2.act`；未观察到 `[SUMMON MONSTER]`、对象级 `[homing]` 或 `.atk [active status]`。辅助对照同 8376/8375 registry 路由、创建块、`.atk` payload 和关键 ANI 盒字段同形；差异为辅助 registry 总量/行号、对象脚本长度以及 `[name]` 由主目标字符串链接样变为直接文本。 |
| `actionobject/SPC/_NewHell/Agaress_Hell` | `darkfireshoot.act` 的 `[CREATE PASSIVEOBJECT] [INDEX] [RANDOM SELECT] 16031/16052/16053` 已逐 ID 回 passiveobject registry 命中 `darkfire1/2/3.obj`；三个候选同构并在攻击成功后创建 16051 `darkfire4.obj`。源发射 BASE/SUB ANI 未观察到盒字段，`darkfire1/2/3/4` BASE ANI 有 `[ATTACK BOX]`，对应 SUB ANI 未观察到盒字段；主目标对象 `[name]` 是字符串链接样。 |
| `actionobject/SPC/_airsword` | 主目标该前缀 5 个文件已做小桶只读复核。`airsword.obj` 挂 `Action/airsword.act` 与 `AttackInfo/body.atk`，width `0 0`、floating height `0`、layer normal、pass all、piercing power `1000`，after-create 声音为 `ELBON_WIND`，销毁块为 `[on end of animation]`；本小桶未观察到对象级 `[homing]`。`airsword.act` 只挂 BASE `../animation/airsword.ani`，未观察到 `[CREATE PASSIVEOBJECT]` 或 `[SUMMON MONSTER]`，因此当前样本无数字 ID registry 解析点。`body.atk` 为 weapon damage apply `1`、damage bonus `100`、physic、no element、down、push aside `100`、lift up `400`、hit horizon、cut、blood `150 1.399999976158142` 等 payload，不提供 hitbox 坐标。`airsword.ani` 3 帧均未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`；同前缀 `airsword_light.ani` 可反编译但未被 `airsword.act` 挂接，也未观察到盒字段。辅助对照同文件数、OBJ/ACT/ATK/ANI 列形、无创建链和无 homing 结论同形，当前未观察到字段差异。 |
| `actionobject/SPC/_bal` | 主目标该前缀 5 个文件已做小桶只读复核。`body.obj` 挂 `Action/body.act` 与 `AttackInfo/body.atk`，width `1 1`、floating height `0`、layer normal、pass all、piercing power `100`，after-create 声音为 `ELBON_WIND`，销毁块为 `[on end of animation]`；本小桶未观察到对象级 `[homing]`。`body.act` 只有一个 `[MOTION]`，BASE 为 `../animation/momentaryslash_red_ldodge_upper.ani`，SUB 为 `../animation/momentaryslash_red_ldodge_under.ani`，SUB 偏移列为 `0 0`；未观察到 `[CREATE PASSIVEOBJECT]` 或 `[SUMMON MONSTER]`，因此当前样本无数字 ID registry 解析点。`body.atk` 为 weapon damage apply `1`、damage bonus `100`、physic、no element、down、push aside `100`、lift up `400`、hit horizon、cut、blood `150 1.399999976158142` 等 payload，不提供 hitbox 坐标。BASE upper ANI 的 FRAME000-004 均有同一组 `[ATTACK BOX] -284 -77 -13 559 157 70`；SUB under ANI 的 FRAME000-004 未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`。辅助对照同 5 文件、OBJ/ACT/ATK/ANI 列形、无创建链、无 homing 和关键 ANI 盒字段同形，当前未观察到字段差异。 |
| `actionobject/SPC/_bat` | 主目标该前缀 9 个文件已做小桶只读复核。`body.obj` 挂 `Action/body.act`、`AttackInfo/attack.atk` 与 `Action/Destroy.act`，width `0 0`、floating height `30`、layer normal、pass all、piercing power `0`、team `100`、after-create 声音 `BAT_FLAP`，销毁块为 `[on end of animation]`。对象有 `[homing]` 闭合块：use `1`、follow `[ENEMY]`、velocity `300 300`、check gap `300`、sync animation rotation `0`、max rotation `180`。`body.act` BASE 为 `../animation/body.ani`，攻击成功后对自身执行行为 0，行为 0 为 `[DESTROY]`；未观察到 `[CREATE PASSIVEOBJECT]` 或 `[SUMMON MONSTER]`，因此当前样本无数字 ID registry 解析点。`Destroy.act` BASE 为 `destroy.ani`，FRAME0 播放 `../Particle/Destroy.ptl`；`.ptl` 的 object 列表引用 `Feather1.ani` / `Feather2.ani` 作为 animation 侧车。`attack.atk` 为 damage `600`、physic、weapon damage apply `0`、dark element、down、push aside `200`、lift up `100`、hit horizontal、cut、blood `30 0.5` 等 payload，不提供 hitbox 坐标。`body.ani` FRAME000-003 均有 `[ATTACK BOX]`，坐标逐帧不同；`destroy.ani`、`feather1.ani`、`feather2.ani` 未观察到盒字段。辅助对照同 9 文件、homing/action/atk/ptl/ANI 盒字段、无创建链和无召唤链同形，当前未观察到字段差异。 |
| `actionobject/SPC/_burnfire` | 主目标该前缀 4 个文件已做小桶只读复核。`body.obj` 挂 `Action/body.act` 与 `AttackInfo/attack.atk`，width `0 0`、floating height `30`、layer normal、pass all、piercing power `0`、team `100`、`[name]` 为字符串链接样 ``<9::weresist_13`火球`>``，after-create 声音 `LANTURN_FIRE -1`，销毁块为 `[on end of animation]`。对象有 `[homing]` 闭合块：use `1`、follow `[ENEMY]`、velocity `400 400`、check gap `100`、sync animation rotation `1`、max rotation `360`。`body.act` BASE 为 `../animation/body.ani`，攻击成功后对自身执行行为 0，行为 0 为 `[DESTROY]`；未观察到 `[CREATE PASSIVEOBJECT]` 或 `[SUMMON MONSTER]`，因此当前样本无数字 ID registry 解析点。`attack.atk` 为 damage bonus `-30`、magic、weapon damage apply `1`、fire element、damage reaction、push aside `100`、lift up `100`、hit horizontal、blow、no blood `30 0.5`，并有两个 `[active status]` payload：`[blind] 100 0 5000 10 0` 与 `[burn] 100 0 15000 2000 500 0 1600`；`.atk` 不提供 hitbox 坐标。`body.ani` 2 帧均有同一组 `[ATTACK BOX] -19 -10 -15 32 20 31`，并有 spectrum / lineardodge 表现字段。辅助对照同 4 文件、homing/action/atk/ANI 盒字段、无创建链和无召唤链同形；差异为辅助 `body.obj [name]` 直接表现为 `火球 ` 文本。 |
| `actionobject/SPC/_aganzo` | 主目标该前缀 3 个文件已做小桶只读复核，只观察到 `Action/body.act`、`AttackInfo/attack.atk` 与 `Animation/body.ani`，未观察到同前缀 `.obj` owner；脚本全文搜索 `_aganzo` 未命中引用，因此本桶登记为 owner 未闭合的 action/atk/ANI 侧车样本，不能写成完整 PassiveObject 对象。`body.act` BASE 为 `../animation/body.ani`，攻击成功后对自身执行行为 0，行为 0 为 `[DESTROY]`；另有默认 off 的时间 trigger：`time + 4000 <= GET TIME` 后执行同一销毁行为；`ON SET ACTION` 后记录 `time = GET TIME` 并打开 trigger `1`。`body.act` 未观察到 `[CREATE PASSIVEOBJECT]` 或 `[SUMMON MONSTER]`，因此当前样本无数字 ID registry 解析点。`attack.atk` 为 damage `1000`、physic、weapon damage apply `0`、attack enemy `1`、no element、damage reaction damage、push aside `100`、lift up `100`、hit wav `NIGHTH_HIT`、hit horizontal、cut、blood `30 0.5`；`.atk` 不提供 hitbox 坐标。`body.ani` 8 帧，FRAME001-003 有 `[ATTACK BOX] -20 -10 -20 40 20 40`，FRAME004 为 `-26 -10 -31 58 20 61`，FRAME005 为 `-37 -10 -37 78 20 76`，FRAME000/006/007 未观察到盒字段。辅助对照同 3 文件、无 `.obj`、action/atk/ANI 盒字段和无脚本引用同形，当前未观察到字段差异。 |
| `actionobject/SPC/_darkcough` | 主目标该前缀 3 个文件已做小桶只读复核。`body.obj` 不走 action，而是以 `[basic motion] Animation/body.ani` 直连 ANI，并挂 `AttackInfo/body.atk`；对象为 width `1 1`、floating height `0`、layer normal、pass all、piercing power `100`，after-create 声音 `ELBON_WIND -1`，销毁块为 `[on end of animation]`。`body.obj` 与 `body.atk` 中未观察到 `[homing]`、`[CREATE PASSIVEOBJECT]` 或 `[SUMMON MONSTER]`；脚本全文搜索 `_darkcough` 未命中引用，`passiveobject/passiveobject.lst` 反向搜索 `_darkcough` 也未命中，因此当前未闭合到数字入口或 registry ID。`body.atk` 为 damage `400`、magic、attack enemy `1`、dark element、damage reaction damage、push aside `30`、lift up `100`、hit horizon、blow、no blood `100 1.0`；`.atk` 不提供 hitbox 坐标。`body.ani` 13 帧，FRAME001 为 `[ATTACK BOX] -30 -20 -10 85 40 58`，FRAME002 为 `-30 -20 -10 60 40 48`，FRAME003-008 均为 `-45 -35 -25 100 70 82`，FRAME000/009-012 未观察到盒字段。辅助对照同 3 文件、OBJ/ATK/ANI 盒字段、无脚本引用和无 registry 反向命中同形，当前未观察到字段差异。 |
| `actionobject/SPC/_cypherelec` | 主目标该前缀 11 个文件已做小桶只读复核。三个 owner `cypherelec.obj`、`cypherelec2.obj`、`cypherelec_item.obj` 同构为 width `0 0`、floating height `30`、layer normal、pass all、piercing power `1000`、team `100`、after-create 声音 `GLARINE_ATK -1`、销毁块 `[on end of animation]`，并有 `[homing]` 闭合块：use `1`、follow `[ENEMY]`、velocity `250 0`、check gap `100000`、sync animation rotation `0`、max rotation `360`；主目标 `[name]` 为字符串链接样 ``<9::name_8011`麥瑟的魔雷閃電`>``，不能当 registry ID。三者分别挂 `Action/body.act` + `AttackInfo/attack.atk`、`Action/body2.act` + `AttackInfo/attack2.atk`、`Action/body3.act` + `AttackInfo/attack_item.atk`。三个 action 均为 BASE ANI + 攻击成功销毁 + `ON SET ACTION` 记录 `time = GET TIME` 并打开 trigger `1`；`body.act` 与 `body3.act` 在 `time + 4000 <= GET TIME` 后销毁，`body2.act` 在 `time + 200 <= GET TIME` 后销毁；本桶 action/obj 未观察到 `[CREATE PASSIVEOBJECT]` 或 `[SUMMON MONSTER]`。`attack.atk` / `attack2.atk` 分别为 damage bonus `-40` / `-50`，`attack_item.atk` 为 absolute damage `1550`，三者均为 magic、no element、cut、blood `30 0.5` 等 payload，不提供 hitbox 坐标。`body.ani` 与 `body2.ani` 均为 8 帧，FRAME001-005 有 `[ATTACK BOX]`，FRAME000/006/007 未观察到盒字段；`body3.act` 使用偏移版 `body2.ani`。脚本全文搜索 `cypherelec` 未命中引用，`passiveobject/passiveobject.lst` 反向搜索 `cypherelec` 也未命中，因此当前未闭合到上游数字入口。辅助对照同 11 文件、OBJ/ACTION/ATK/ANI 盒字段、无脚本引用和无 registry 反向命中同形；差异为辅助 `[name]` 是直接文本。 |
| `actionobject/SPC/_cyclone` | 主目标该前缀 16 个文件已做小桶只读复核。六个 owner 均为 width `2 2`、floating height `0`、layer normal、pass all、piercing power `1000`、team `100`、销毁块 `[on end of animation]`，主目标 `[name]` 为字符串链接样 ``<9::name_8015`天谴飓风 2`>``，不能当 registry ID。`body.obj`、`short1.obj`、`short2.obj` 均挂 `Action/HurricaneOfJudgement.act`，分别挂 `HurricaneOfJudgement.atk`、`Short1.atk`、`Short2.atk`，并有 homing：follow `[ENEMY]`、velocity `150 150`、check gap `500`、sync animation rotation `0`、max rotation `360`；`goldteida_effect.obj` 同 action、挂 `HurricaneOfJudgement2.atk`，homing 为 velocity `250 250`、check gap `300`、sync `0`、max `360`。`short1_1.obj` / `short2_1.obj` 只挂 `Action/HurricaneOfJudgement_opt.act`，未观察到对象级 `[attack info]` 或 `[homing]`。`HurricaneOfJudgement.act` BASE `HurricaneOfJudgement.ani`，有 `ON SET ACTION -> time = GET TIME -> TRIGGER 1 ON`，`time + 10000 <= GET TIME` 后销毁，并有 FRAME10 限次 `[SET FRAME] 6`；本 action 未观察到 CREATE 或 SUMMON。`HurricaneOfJudgement_opt.act` BASE `HurricaneOfJudgement_3.ani`，FRAME0/1/2 均创建 48117，FRAME3 销毁；48117 回 `passiveobject/passiveobject.lst` 命中 `ActionObject/SPC/_Cyclone/Short1.obj`。`HurricaneOfJudgement.atk` / `HurricaneOfJudgement2.atk` 分别为 damage bonus `-50` / `100`；`Short1.atk` / `Short1_1.atk` 为 absolute damage `800`，`Short2.atk` / `Short2_1.atk` 为 absolute damage `400`，但 `Short1_1.atk` / `Short2_1.atk` 在当前样本 owner 搜索未命中，不能自动挂到 `_1.obj`。`HurricaneOfJudgement.ani` 11 帧均有 `[ATTACK BOX]`，多帧同帧多条攻击框；`HurricaneOfJudgement_3.ani` 4 帧为空图，未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`。辅助对照同 16 文件、48117 registry 目标、action/atk/ANI 盒字段同形；差异为辅助 `[name]` 是直接文本，registry 总量/行号不同。 |
| `actionobject/SPC/_avengers` | 主目标该前缀 58 个文件已做小桶只读复核。`alectoball` 的 `last.act` 创建 46004，回 `passiveobject/passiveobject.lst` 命中 `Common/LightExplosion.obj`；`firerain` 的 `body.act` 创建 8387，回同 registry 命中 `ActionObject/Common/ExpLand.obj`；两者在 `monster/monster.lst` 未命中。`alectoreviver`、`tisponereviver`、`megairareviver` 使用 `[SUMMON MONSTER] [INDEX] 61347/61348/61349`，三者回 `monster/monster.lst` 命中 Avengers 三个 `.mob`，在 passiveobject registry 未命中。`alectoball` / `firerain` owner 和下游对象均有攻击框正样本；reviver 侧是召唤/检查边界，部分 owner ANI 无盒或仅 damage box。辅助对照同文件数、同关键 ID 路由和核心列形可读，主要差异为 registry 总量/行号与 name 字符串样式。 |
| `actionobject/SPC/_cursequeen/void` | 主目标该前缀 22 个文件已做小桶只读复核。`body.obj`、`pursuit1.obj`、`pursuit2.obj` 均有 `[homing]` 闭合块：`[homing use] 1`、`[homing follow] [ENEMY]`、`[homing velocity] 50 50`、`[homing check gap] 200`、`[sync animation rotation] 0`、`[homing time] 0`；三者均有 `[object destroy condition] [destroy condition] [time limite] 5000`、`[team] 100` 和 after-create 声音。`body.obj` 使用 `attack.atk`，`pursuit1/2.obj` 分别使用 `pursuit_attack1/2.atk`；动作链为 start -> loop -> end，loop action 在 FRAME0 执行 `[ATTACKRECT] [RESET]`，end action 在 FRAME2 `[DESTROY]`。本小桶未观察到 `[CREATE PASSIVEOBJECT]` 或 `[SUMMON MONSTER]`，因此当前样本无数字 ID registry 解析点。lineardodge BASE start/loop ANI 有 `[ATTACK BOX]`，normal SUB 和 end lineardodge ANI 未观察到盒字段。辅助对照同文件数、同核心 homing/action/atk/ANI 形态可读，主要差异为 name 字符串样式。 |
| `actionobject/SPC/_cursequeen/spire` | 主目标该前缀 6 个文件已做小桶只读复核：`body.obj`、`Action/body.act`、`AttackInfo/attack.atk`、`Animation/body.ani`、`Animation/belzebuite_overskill_dark.ani`、`Particle/belzebuite_overskill_dark.ptl`。`body.obj` 为 width `0 0`、floating height `50`、layer normal、pass all、piercing power `1000`、team `100`，挂 `Action/body.act` 与 `AttackInfo/attack.atk`，并通过 particle 引用 `belzebuite_overskill_dark.ptl`；after-create 声音为 `SPIRE_CRASH`，销毁块为 `[destroy condition] [time limite] 5000`。`body.act` 只有 MOTION BASE `../Animation/body.ani`。`attack.atk` 为 magic / dark element，damage bonus `50`、weapon damage apply `1`、attack enemy `1`，并有 curse、slow、armor break 三段 `[active status]` payload；`.atk` 不提供 hitbox 坐标。`body.ani` 单帧有 `[ATTACK BOX] -178 -100 -13 348 200 81`；粒子挂接的 dark ANI 未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`。本小桶 6 个文件内未观察到 `[CREATE PASSIVEOBJECT]`、`[SUMMON MONSTER]` 或 homing；主目标 `passiveobject.lst` 和 NUT 文本搜索未观察到本路径直接入口，因此当前样本无数字 ID registry 解析点。辅助对照同 6 文件、同核心 obj/action/atk/ANI/particle 形态，未见覆盖主目标结论的结构差异。 |
| `actionobject/SPC/_cursequeen/cry` | 主目标该前缀 20 个文件已做小桶只读复核。`body.obj` 与 `body_tower.obj` 同为 width `0 0`、floating height `50`、layer normal、pass all、piercing power `1000`、team `100`，均挂 `AttackInfo/attack.atk`，并有四条粒子侧车：part / inazuma / exp / dark；粒子 create delay 分别为 `200`、`200`、`1200`、`200`，exp 粒子带 create sound `BELZEBUITE_SKILL_CRASH`。`body.obj` 走 `Action/body.act` 且 time-limit 销毁 `10000`；`body_tower.obj` 走 `Action/body_Tower.act` 且 time-limit 销毁 `4500`；二者 after-create 声音均为 `FT_AVATAR_HORROR_03`。两个 action 内容同形：BASE `../Animation/body.ani`，FRAME0 触发一次 `FLASH SCREEN 150 1500 150 200 30 30 30`，并执行 `[ATTACKRECT] [RESET]`。`attack.atk` 为 magic / dark element、weapon damage apply `1`、attack enemy `1`、blow / no blood `50 1.0` / push aside `100` / lift up `100`，并有 curse `[active status]` 列 `100 60 10000 100 100 100 100`；`.atk` 不提供 hitbox 坐标。`body.ani` 为 2 帧 loop 空图，FRAME000-001 均有 `[ATTACK BOX] -155 -46 -65 305 50 129`。四条粒子挂接的 particle / inazuma / exp / dark ANI 未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`；同目录 `circle_of_magic_1/2/3.ani` 可读但当前未在 owner/action/particle 链中观察到挂接。文本链内未观察到 `[CREATE PASSIVEOBJECT]`、`[SUMMON MONSTER]` 或 homing；主目标 `passiveobject.lst` 和 NUT 文本搜索未观察到本路径直接入口，因此当前样本无数字 ID registry 解析点。辅助对照同 20 文件、同核心 owner/action/atk/body ANI/无 create/无 summon/无 homing 形态，未见覆盖主目标结论的结构差异。 |
| `actionobject/SPC/_firewitch/ador` | 主目标该前缀 6 个文件已做小桶只读复核。`body.obj` 挂 `Action/body.act`、`AttackInfo/attack.atk` 与 `Action/Destroy.act`，并有 `[homing]` 闭合块：follow `[ENEMY]`、velocity `250 250`、check gap `600`、sync animation rotation `0`、max rotation `180`；销毁块为 `[on end of animation]`。`body.act` 在攻击成功后 `[DESTROY]`，`body.ani` 4 帧均有 `[ATTACK BOX] -46 -19 -10 69 38 48`；`Destroy.act` 在 FRAME0 创建 40002 并可召唤 601，40002 必须按 passiveobject registry 命中 `Common/FireExplosion.obj`，601 必须按 monster registry 命中 `Spirit/FireSpirit.mob`。40002 和 601 在错误 registry 中也有碰撞命中，不能脱离父块解释；`Destroy.ani` 未观察到盒字段，下游 `FireExplosion.ani` 空图帧有 `[ATTACK BOX] -65 -40 -50 130 80 100`。辅助对照同路径核心结构、ID 路由和 ANI 盒字段同形；主要差异为下游 `FireExplosion.obj [name]` 主目标是字符串链接样，辅助为直接文本，registry 总量/行号不同。 |
| `actionobject/SPC/_firewitch/firebreath` | 主目标该前缀 4 个文件已做小桶只读复核。`body.obj` 挂 `Action/Body.act` 与 `AttackInfo/Body.atk`，floating height `50`、pass all、piercing power `1000`，销毁块为 `[on end of animation]`；本小桶未观察到对象级 `[homing]`。`Body.act` 只挂 BASE `../Animation/Body.ani`，在 FRAME5/7/9/11/13/15/17 对自身执行 `[ATTACKRECT] [RESET]`，未观察到 `[CREATE PASSIVEOBJECT]` 或 `[SUMMON MONSTER]`。`Body.atk` 为 magic / fire / weapon damage apply `1` / damage bonus `-50` 等 payload，不提供 hitbox 坐标。`Body.ani` FRAME000-017 均有 `[ATTACK BOX]`，FRAME018-019 未观察到盒字段，部分帧同帧多条攻击框。辅助对照同文件数、action、atk 和 ANI 盒字段同形；差异为主目标 `[name]` 是字符串链接样且 after-create 声音为 `NPC_SERIA_AMB_02`，辅助为直接文本且声音为 `FIRE_02_LOOP`。 |
| `actionobject/SPC/_firewitch/meteo` | 主目标该前缀 21 个文件已做小桶只读复核。`body.obj`、`horobe1.obj`、`horobe2.obj` 共用 `Action/body.act` 与 `Action/Destroy.act`，分别挂 `attack.atk`、`horobe1.atk`、`horobe2.atk`；`body_cyclops.obj` 单独走 `Action/body_Cyclops.act`、`Action/Destroy_Cyclops.act` 与 `attack2.atk`。四个对象均为 on-end-animation 销毁、after-create `MATEO_FLY`、floating height `50`、pass all、piercing power `1000`；本小桶未观察到对象级 homing。`body.act` / `body_Cyclops.act` 在 FRAME0 闪屏，并在 `ZPOS <= 40` 检查后 `[SET FRAME] 1`；`body.act` 另挂 `glow.ani` SUB。源 body ANI 有 `[ATTACK BOX]`，`destroy.ani` / `destroy_cyclops.ani` 未观察到盒字段。`Destroy.act` 创建 8236，回 passiveobject registry 命中 `ActionObject/Monster/Jeff/Jeff_Omega_BigBoom.obj`；`Destroy_Cyclops.act` 创建 10027，回同 registry 命中 `ActionObject/Monster/Jeff/BigBoom.obj`。8236 下游对象未观察到对象级 `.atk`，其 BASE/SUB ANI 样本无盒；10027 下游对象挂 `Big.atk`，BASE `Bigboom1_1.ani` FRAME000-004 有 `[ATTACK BOX]`。辅助对照同文件数、入口对象、CREATE ID 目标、`.atk` 列形和关键 ANI 盒字段同形；主要差异为 passiveobject registry 总量/行号不同。 |
| `actionobject/SPC/rina` | 主目标该前缀 18 个文件已做小桶只读复核。`event_thunder.obj`、`rocketblast.obj`、`rosiclockball.obj` 分别挂 `Action/Event_Thunder.act` + `AttackInfo/Event_Thunder.atk`、`Action/RocketBlast.act` + `AttackInfo/RocketBlast.atk`、`Action/RosiClockBall.act` + `AttackInfo/RosiClockBall.atk`；`skypotel.obj` 只挂 `Action/skypotel.act`，未观察到对象级 `[attack info]`。`rosiclockball.obj` 有 `[homing]` 闭合块：follow `[ENEMY]`、velocity `10 10`、check gap `100`、sync animation rotation `1`、max/init/diff rotation `360/360/360`、homing time `50000`；`[ENEMY]` 后无数字，不解析 registry。四个 action 未观察到 `[CREATE PASSIVEOBJECT]` 或 `[SUMMON MONSTER]`；`skypotel.act` 在 FRAME0/1 对 `[SUMMON MASTER]` 执行行为 0，行为 0 为 `[SET SPEED]` X `1200`、Z `600`，这不是 monster 召唤块。`Event_Thunder.ani` FRAME000-006 有攻击框、FRAME007-009 无盒；`RocketBlast1.ani` FRAME001-003 有攻击框，`RocketBlast2.ani` FRAME000-004 有攻击框；`RosiClockBall.ani` 4 帧均有攻击框；`skypotel.ani` 3 帧为空图无盒。`Archer_atk10.atk` 文件存在，但主目标 owner 搜索只命中 common `goblin_arrow10.obj`，不能写成 rina owner。`effect.ptl` 指向的相对 `Animation/StoneSmoke.ani` 在 rina 前缀未命中，不能用其他前缀同名 ANI 替代。`passiveobject.lst` 反向搜索未观察到 rina 登记，因此当前未闭合到数字入口。辅助对照同 18 文件、核心 action/atk/ANI/homing/无 CREATE/无 SUMMON 形态同形；差异为辅助带 `[name]` 的两个对象使用直接文本，主目标使用 string-link 样。 |
| `actionobject/SPC/laowu` | 主目标该前缀 13 个文件已做小桶只读复核。`laowu.obj` 为 width `0 0`、floating height `0`、layer normal、pass all、piercing power `1000`，只挂 `Action/buff.act`，未观察到对象级 `[attack info]`、`[homing]`、`[object destroy condition]`、`[CREATE PASSIVEOBJECT]` 或 `[SUMMON MONSTER]`。`buff.act` 挂 BASE `buff_normal_front.ani`、SUB `buff_front.ani`，以及 `SUB ANI WITH XYZ` 的 `buff_back.ani` / `buff_normal_back.ani` 组合；`CHECK TIME 500` 后按 `[WHICH] [AI CHARACTER] [IS INDEX] 21401` 检查，21401 回 `aicharacter/aicharacter.lst` 命中 `aicharacter/priest/laowu/laowu.aic`，不是 passiveobject 或 monster registry。检查未通过时对自身执行行为 0 `[DESTROY]`。`empty01.act` 文件存在，BASE/SUB 为 `SpecialBalloon.ani` / `AvalancheBalloon.ani`，FRAME1 后销毁，但主目标 owner 搜索未命中，不能挂到 `laowu.obj`。action-linked 四个 buff ANI 和相邻可读 `buff_normal.ani`、`empty.ani`、`skill_effect.ani`、`skill_light.ani` 均未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`；`passiveobject.lst` 反向搜索未观察到 laowu 登记。辅助对照同 13 文件、owner/action/ANI/no CREATE/no SUMMON/no attackinfo/no homing 同形；差异为 21401 仍指向同一 aicharacter raw path，但 registry 总量/行号不同。 |
| `actionobject/SPC/mesteria` | 主目标该前缀 20 个文件已做小桶只读复核。`halloweenbuster.obj` 通过 11012 回 `passiveobject/passiveobject.lst` 命中 `ActionObject/SPC/mesteria/HalloweenBuster.obj`，同号在其他 registry 有碰撞，必须按 passiveobject 上下文解释；对象为 floating height `1`、normal layer、pass all、piercing power `1000`，挂 `Action/HalloweenBuster1.act`、`AttackInfo/HalloweenBuster.atk` 与两个 etc motion，未观察到对象级 `[homing]`、`[object destroy condition]` 或 `[SUMMON MONSTER]`。`HalloweenBuster1.act` BASE 为 `HalloweenBuster1.ani`，SUB 为 `HalloweenBuster2.ani` / `HalloweenBuster3.ani`；`ZPOS <= 5` 且 limit `1` 时创建 40005、空 particle、level `70`、pos `0 0 0`，随后 `[DESTROY]`，FRAME0/1/2 另播放 8 组 `ShootingStarFire.ptl` 粒子。40005 回 passiveobject registry 命中 `Common/ShootingStarExp.obj`，同号也有 equipment/monster/map 碰撞，创建块上下文仍只按 passiveobject 解释。源 `.atk` 为 absolute damage `2000`、magic、weapon damage apply `1`、attack enemy `1`、fire、damage reaction damage；下游 `ShootingStarExp.atk` 主目标仅观察到 magic / attack enemy / fire / down / hit lift up / blow / no blood / push aside / lift up，未观察到 damage bonus 或 weapon damage apply。`HalloweenBuster1.ani` 四帧均有 `[ATTACK BOX] -25 -14 -28 54 28 57`；`HalloweenBuster2.ani` / `HalloweenBuster3.ani` 未观察到盒字段；40005 下游 `ShootingStarExp.ani` 单帧空图有 `[ATTACK BOX] -100 -30 -20 200 60 40`。同前缀 `HalloweenBusterSub.atk` / `HalloweenBusterSub.ani` 可读，且 sub ANI 有攻击框，但 owner 搜索只命中 `character/mage/halloweenbustersub.obj`，不能挂到 mesteria owner。辅助对照同 20 文件、核心 action/atk/ANI/40005 路由同形；差异为 name 文本样式、registry 总量/行号不同，且辅助对照 common `ShootingStarExp.atk` 额外有 damage bonus `3800` 和 weapon damage apply `1`，不能覆盖主目标结论。 |
| `actionobject/monster/timegate/TimeLord` | `Cube_L`、`Chain` / `Chain2`、`magic_L` 已做主目标只读闭合；16075 创建 16080 走 passiveobject registry，`Chain_attack_loop.act` 中 `[WHICH] [MONSTER] [IS INDEX] 64013` 走 `monster/monster.lst`。Cube / magic 为无盒表现样本，Chain 多个 BASE/SUB ANI 有 `[ATTACK BOX]` 或 `[DAMAGE BOX]`。 |
| `actionobject/monster/Blood_An` | `darkfireshoot.act` 的 `[CREATE PASSIVEOBJECT] [INDEX] [RANDOM SELECT] 8718/8719/8720` 已逐 ID 回 passiveobject registry 命中 `darkfire1/2/3.obj`；三个候选同构并在攻击成功后创建 8725 `darkfire4.obj`。源发射 ANI 无盒，`darkfire1/2/3/4` BASE ANI 有 `[ATTACK BOX]`，对应 SUB ANI 未观察到盒字段。 |
| `actionobject/monster/Grim/Grim_seeker` | `fire2.act` 的 `[CREATE PASSIVEOBJECT] [INDEX] [RANDOM SELECT] 8679/8680` 命中 `FireReady.obj` / `FireReady2.obj`；`fire2_r.act` 的 9136/9137 命中 `FireReady_Riding.obj` / `FireReady_Riding1.obj`。8679 与 9136 的 basic action 可静态回到源创建 action，8680 与 9137 走 `Fire3.act`；`Fire2.ani` 24 帧均有 `[ATTACK BOX]`，`FireBall.atk` / `FireBall_r.atk` 分别为 magic/fire/burn 与 physic/fire/burn。`[WHICH] [MONSTER] [IS INDEX] 61150` 走 `monster/monster.lst`。 |
| `actionobject/monster/gashengrigun/ez8_1 -> 8588` | `ez8_1.obj` 通过 `[etc action] Action/Boom2.act` 进入同目录物理 `boom2.act`；该 action 第 2 帧对 `[CHARACTER]` 条件创建 `INDEX 8588`，已回 `passiveobject/passiveobject.lst` 命中 `Monster/ZealotRebirth/NenGuard_1.obj`。`gashengrigun/Boom2.ani` 与 `EZ8_2.act` 的 CountDown/Stay3 有 `[DAMAGE BOX]`，Dust SUB 无盒；下游 `NenGuard.ani` 多帧多条 `[DAMAGE BOX]`，`NenGuardFloor/Effect` 未观察到盒字段。辅助对照同链可读，但 `NenGuard_1.obj [int data]` 数值与主目标不同，不能覆盖主目标。 |
| `actionobject/monster/gashengrigun` 前缀闭合 | 主目标该前缀 51 个文件已做只读闭合。`ez8.obj` 走 `EZ8.act` 与 `Boom.act`，`Boom.act` 创建 8794 `ActionObject/Common/BigBoom4.obj`；`g3/g3_1/g3_2/g3_3` 走 `Attack*.act` 并通过 `[SUMMON MONSTER]` 召唤 61134/61137，相关 ID 走 `monster/monster.lst`；`gasbomb.obj` 走 `GasBomb.act`，BASE ANI 有攻击框并创建 8591 `SleepingGas.obj`；`sleepinggas/sleepinggas2/sleepinggas3_weapon` 挂 `Gas/Gas2/Gas3_weapon.atk`，三者均为 sleep 三数值列；`summonrx78.obj` 的 `Summon.act` 同时创建 8561 passiveobject 并召唤 61136 monster。辅助对照同文件清单和关键 registry raw path 可读，主要差异为对象 `[name]` 直接文本与主目标 string-link 样式。 |
| `actionobject/monster/lizardman + guardiangoblin -> 8568` | 主目标 8568 已回 `passiveobject/passiveobject.lst` 命中 `ActionObject/Common/GuardianGrenadeNoneBomb.obj`。`lizardman/summonstonebody.act` 同一行为块创建 8568 并召唤 61129-61133 monster；`lizard_stonebody2.act` 也创建 8568 并检查 61129。`guardiangoblin` 下 `realspearjail1/2`、`spearjail`、`spearjail3`、`spearjailready` 均观察到 8568 入边，且多处同块创建 8561。8568 下游 `.atk` 为 magic/no element/down，BASE `GrenadeNoneBomb.ani` 的 `FRAME000` 到 `FRAME006` 有两条 `[ATTACK BOX]`，SUB `GrenadeNoneBombEffect.ani` 未观察到盒字段。辅助对照同主链可读，但 8568 额外观察到 `metrocenter/hideout_of_fear/gass_bon` 入边，不能写成主目标存在。 |
| `actionobject/monster/new_event/CypherElec0 / CypherElec1` | 主目标 16068 和 16152 均经 `passiveobject/passiveobject.lst` 命中 `ActionObject/Monster/New_Event/CypherElec0.obj` / `CypherElec1.obj`；同号另有 equipment registry 碰撞，只有 `[CREATE PASSIVEOBJECT] [INDEX]` 上下文按 passiveobject 解释。`CypherElec0.obj` 为 width `0 0`、floating height `30`、normal layer、pass all、piercing power `1000`、team `100`，basic action `Action/body0.act`，attack info `AttackInfo/attack0.atk`，after-create sound `GLARINE_ATK -1`，time limit `10000`；`CypherElec1.obj` 同形但 basic action / attackinfo 为 `body1.act` / `attack1.atk`，并有 homing：use `1`、follow `[ENEMY]`、velocity `250 0`、check gap `100000`、sync animation rotation `0`、max rotation `360`。`body0.act` BASE `body0.ani`，FRAME0 限 3 次设置随机 X/Y 速度并使用自身方向，攻击成功或 `time + 7000 <= GET TIME` 后自毁；`body1.act` BASE `body1.ani`，攻击成功或 `time + 4000 <= GET TIME` 后自毁；二者均在 ON SET ACTION 记录 `time = GET TIME` 并打开 trigger 1。`attack0.atk` / `attack1.atk` 同形，为 damage `1000`、magic、weapon damage apply `1`、attack enemy `1`、light element、damage reaction damage、push/lift `100/100`、hit wav `R_FG_HIT`、hit horizontal、cut、blood `30 0.5`，不提供 hitbox 坐标。`body0.ani` 与 `body1.ani` 均为 8 帧同形：FRAME001-003 为 `[ATTACK BOX] -20 -10 -20 40 20 40`，FRAME004 为 `-26 -10 -31 58 20 61`，FRAME005 为 `-37 -10 -37 78 20 76`，FRAME000/006/007 未观察到盒字段。主目标上游创建只闭合 16068：`dual_face/p1_attack.act`、`dual_face/p2_attack.act` 与 `dual_face2/action/attack.act` 均在 `[CREATE PASSIVEOBJECT] [INDEX] 16068` 上下文创建，创建块为空 particle、level `1`、随机 POS，并带 `COLLISION GROUP 2 0 1`；当前未观察到 16152 的主目标创建入口。限定检索中，本桶 passiveobject/action/atk 源文件未观察到继续 `[CREATE PASSIVEOBJECT]`、`[SUMMON MONSTER]` 或 `.atk [active status]`；homing 只命中 `CypherElec1.obj`。`body1_ex.act` 可读但未在本桶 owner/action 文本中观察到挂接，只登记为相邻未挂接边界。辅助对照同 16068/16152 passiveobject 路由、核心 obj/action/atk/ANI 盒字段同形；差异为对象 `[name]` 直接文本、registry 总量/行号、对象脚本长度不同，且辅助只观察到 `p1_attack.act` / `p2_attack.act` 两个 16068 上游创建，未观察到主目标 `dual_face2` 路径。 |
| `actionobject/monster/new_event/AirSword00` | 主目标 16155 经 `passiveobject/passiveobject.lst` 命中 `ActionObject/Monster/New_Event/AirSword00.obj`；同号另有 equipment registry 碰撞，只有 `[CREATE PASSIVEOBJECT] [INDEX]` 上下文按 passiveobject 解释。`AirSword00.obj` 为 floating height `1`、normal layer、pass all、piercing power `1000`，basic action `action/AirSword_start.act`，attack info `AttackInfo/AirSword.atk`，etc action `action/AirSword_stay.act`，time limit `10000`；对象未观察到 `[width]`、`[homing]` 或对象级 team 字段。`AirSword_start.act` BASE `Airsword00.ani`，FRAME0/7/15/20/25/32/40/45/50 执行 `[ATTACKRECT] [RESET]`，FRAME53 切 `[SET ACTION] [CUSTOM] 0 [NOW]`；`AirSword_stay.act` BASE `Airsword00_stay.ani`，含距离 `<= 600` 的 `[WHICH] [CHARACTER]` 检查、`[PULL APPENDAGE] 4 4 8000/7000/6000` 和 X 轴速度 `1000/500/200/0` 的 `[USE MY DIRECTION]` 行为。`AirSword.atk` 为 absolute damage `250`、physic、weapon damage apply `1`、attack enemy `1`、dark element、damage reaction none、push/lift `0/0`、hit wav `R_DARK_KNIGHT_HIT`、hit horizontal、cut、blood `30 1`，不提供 hitbox 坐标。`Airsword00.ani` 为 55 帧，`Airsword00_stay.ani` 为 62 帧；二者每帧均观察到同形 `[ATTACK BOX] -72 -20 -28 141 40 44`，未观察到 `[DAMAGE BOX]`。主目标上游创建闭合四个入口：`dual_face/p1_attack2_l.act` 和 `p2_attack2_r.act` 在 FRAME5 创建 16155，创建块为空 particle、level `1`、POS `75 0 0`、`COLLISION GROUP 2 0 1`，且其 `[WHICH] [MONSTER] [IS INDEX]` 分别检查 61650/61656，二者走 `monster/monster.lst`；`dual_face2/action/attack2_l.act` 与 `attack2_r.act` 在 FRAME0 分别设 left/right 后创建 16155，创建块为空 particle、level `1`、POS `5 0 0`、`COLLISION GROUP 2 0 1`。限定检索中，本桶 passiveobject/action/atk 源文件未观察到继续 `[CREATE PASSIVEOBJECT]`、`[SUMMON MONSTER]`、`.atk [active status]` 或 `[homing]`。辅助对照同 16155 passiveobject 路由、核心 obj/action/atk/ANI 前段盒字段同形；差异为对象 `[name]` 直接文本、registry 总量/行号、对象脚本长度不同，且辅助只观察到 `dual_face/p1_attack2_l.act` / `p2_attack2_r.act` 两个上游创建，未观察到主目标 `dual_face2/action/attack2_l.act` / `attack2_r.act` 两个入边，并额外出现 skill 侧数字命中，只能作为目标集差异提示。 |
| `actionobject/monster/new_event/Missile0` | 主目标 16153 经 `passiveobject/passiveobject.lst` 命中 `ActionObject/Monster/New_Event/Missile0.obj`；同号另有 equipment registry 碰撞，本桶对象 ID 解析以 passiveobject registry 为准，不能用 equipment 命中覆盖。`Missile0.obj` 为 floating height `1`，basic action `action/Missile0.act`，attack info `AttackInfo/BossMissile0.atk`，last action `Action/body1_EX.act`，time limit `30000`，after-create sound `TEMPESTER_MISSILE -1`；对象未观察到 `[width]`、`[layer]`、`[pass type]`、`[piercing power]`、team 或 `[homing]` 字段。`Missile0.act` BASE `BossMissile0.ani`，FRAME0/1 执行烟雾粒子 `BossMissileSmog0.ptl`，FRAME2 先执行同一粒子再执行 `[ATTACKRECT] [RESET]`。`BossMissile0.atk` 为 weapon damage apply `1`、damage bonus `0`、magic、attack enemy `1`、fire element、damage reaction damage、blow、no blood `50 1.0`、push aside `300`、lift up `450`，不提供 hitbox 坐标。`BossMissile0.ani` 为 3 帧，FRAME000-002 均有同形 `[ATTACK BOX] -24 -10 -9 49 20 18`；`body1_EX.act` 的 BASE `1ExpDodge_M.ani` 与 SUB `1ExpNormal_M.ani` 均为 11 帧表现 ANI，未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`，FRAME10 执行 `[DESTROY]`；烟雾 `BossMissileSmog0.ptl -> BossMissileSmog0.ani` 为 7 帧表现侧车，未观察到盒字段。主目标全 PVF 数字检索未观察到 16153 的 `[CREATE PASSIVEOBJECT]` 创建入口，只命中 registry / itemdictionary / iteminfo / mercenary 等数字表；限定源文件中未观察到继续 `[CREATE PASSIVEOBJECT]`、`[SUMMON MONSTER]`、`.atk [active status]` 或 `[homing]`。辅助对照同 16153 passiveobject 路由、核心 obj/action/atk/ANI 同形，反向数字命中同为 registry / itemdictionary / iteminfo / mercenary 等数字表；差异为对象 `[name]` 直接文本、registry 总量/行号和对象脚本长度不同，只能作为目标集差异提示。 |
| `actionobject/monster/new_event/Assault_Missile0` | 主目标 16154 经 `passiveobject/passiveobject.lst` 命中 `ActionObject/Monster/New_Event/Assault_Missile0.obj`；同号另有 equipment registry 碰撞，本桶对象 ID 和创建块均按 passiveobject registry 解释。`Assault_Missile0.obj` 为 width `1 1`、normal layer、pass type `[do not pass]`、piercing power `0`、team `100`，basic action `action/Assault_Missile0.act`，attack info `AttackInfo/Assault_Missile0.atk`，last action `Action/body1_EX.act`，time limit `30000`，并有 homing：use `1`、follow `[ENEMY]`、velocity `500 500`、check gap `5000`、sync animation rotation `0`、max/init rotation `0/0`、diff rotation `190`、homing time `1000`、sync target move。`Assault_Missile0.act` BASE `Missile10.ani`、SUB `Missile20.ani`，FRAME0 限 1 次设置 X/Z 速度 `-20/-40` 且使用自身方向，FRAME1 限 1 次设置 X/Z 速度 `900/0` 且使用自身方向；action 内未观察到 `[ATTACKRECT] [RESET]`。`Assault_Missile0.atk` 为 damage `1200`、damage bonus `2`、weapon damage apply `1`、physic、attack enemy `1`、no element、damage reaction damage、push/lift `50/50`、hit wav `A_WHEEL_HIT`、hit down、blow、no blood `0 1.0`，不提供 hitbox 坐标。`Missile10.ani` 为 2 帧，FRAME000 为 `[ATTACK BOX] -14 -10 -6 32 20 13`，FRAME001 为 `-14 -10 -7 32 20 13`；`Missile20.ani` 为 8 帧表现 SUB，未观察到盒字段。复用 last action `body1_EX.act` 的 `1ExpDodge_M.ani` / `1ExpNormal_M.ani` 均为 11 帧表现 ANI，未观察到盒字段，FRAME10 执行 `[DESTROY]`。主目标上游创建闭合两个入口：`zx_77/action/casting.act` 和 `casting1.act` 在 FRAME4 与 FRAME6 各创建 16154，创建块为空 particle、level `1`、POS 分别为 `0 5 -1` 与 `10 -5 -1`、`USE OBJECT ZPOS`、`COLLISION GROUP 2 0 1`，二者差异为 `FIX DIRECTION LEFT/RIGHT`；FRAME6 创建后还设置 X speed `-30`，FRAME7 设置 X speed `0`。限定源文件内未观察到继续 `[CREATE PASSIVEOBJECT]`、`[SUMMON MONSTER]` 或 `.atk [active status]`；`[homing]` 只命中对象本体。辅助对照同 16154 passiveobject 路由、核心 obj/action/atk/ANI 同形；差异为对象 `[name]` 直接文本、registry 总量/行号、对象脚本长度不同，并且辅助额外观察到 `timespiral/zx_77/action/casting.act` / `casting1.act` 与 atgunner skill 数字命中，只能作为目标集差异提示，不能覆盖主目标入边结论。 |
| 枪手目录 12 个 PVP `.atk` | 已闭合到 owner `.obj`；普通枪械多走 `[attack info]`，手雷和 Tempester 导弹爆炸可走 `[etc attack info]`；对应 ANI 有 `[ATTACK BOX]` 正样本，见 `indexes/attackinfo-pvp-gunner-chain-ledger.zh-CN.md`。 |
| Fighter 目录 4 个 PVP `.atk` | 已闭合到 owner `.obj`；野蛮冲撞终段走 `[etc attack info]`；对应 ANI 有 `[ATTACK BOX]` 正样本，且部分视觉 / dodge / etc motion 已读但未见攻击框，见 `indexes/attackinfo-pvp-fighter-chain-ledger.zh-CN.md`。 |
| Mage 目录 4 个 PVP `.atk` | 已闭合到 owner `.obj` 并审计 owner-listed motion；`ATBrokenArrow` 有 owner basic motion `[ATTACK BOX]`，其余样本存在 owner-listed motion 无攻击框或相邻 ANI 未直接挂接的 hitbox 映射风险，见 `indexes/attackinfo-pvp-mage-chain-ledger.zh-CN.md`。 |
| Priest / Swordman / Thief 小桶 6 个 PVP `.atk` | 已闭合到 owner `.obj`；`DevilStrike1`、`Judgement`、`OutRageBreakFloor`、`OutRageBreakFloor_DS`、`Ballacre attack_pierce` 与 `BoneShield bone_rotate_none` 均有 `[ATTACK BOX]` 正样本，见 `indexes/attackinfo-pvp-priest-swordman-thief-chain-ledger.zh-CN.md`。 |
| Common / Equipment 5 个 PVP `.atk` | 已闭合到目标目录 owner `.obj/.act/.ani`；`FireExplosion`、`boom01`、`fire_boom_e1`、`TalismanThunderbolt1` 有 `[ATTACK BOX]` 正样本；`Event_Thunder` 父对象自身为空图并创建 registry 48234 下游对象，见 `indexes/attackinfo-pvp-common-equipment-chain-ledger.zh-CN.md`。 |
| zrr_skill 9 个 PVP `.atk` | 已完成静态只读审计；1 个闭合到 `.obj/.atk` 但 owner-listed ANI 在主目标搜索和直接读取中断链，辅助同路径也未命中；8 个未在全 PVF 脚本文本中找到 owner 字符串，见 `indexes/attackinfo-pvp-zrr-skill-chain-ledger.zh-CN.md`。 |
| `actionobject/SPC/root-icedrop-managers` | 主目标 root SPC IceDrop/manager 小桶已做只读复核。`flowermanager.obj` 挂 `Action/Flowermanager.act`，对象级有 `[sound category] [after object create] NPC_SERIA_AMB_02 -1` 与 `[object destroy condition] [on end of animation]`；其 action 在 FRAME0 连续 7 次执行同一行为创建 8153，particle 为 `../Particle/dummy2.ptl`，level `40`，pos 为两段 `[RANDOM] -200 200` 与 `[RANDOM] -90 90 0`，随后自毁。8153 按 `passiveobject/passiveobject.lst` 命中 `ActionObject/BreakableObject/IceFlower.obj`，同号有 stackable 碰撞；IceFlower 只作为 8153 check/create 边界目标记录，最小字段为 width `54 32`、floating height `0`、pass type `[do not pass]`、hp max `8000`、hp destroy `1`、destroy particle `Particle/IceFlowerDestroy.ptl`。`icedropmanager.obj` 挂 `Action/IceDropManager.act`；action 在 FRAME0 用 `[WHICH] [PASSIVE] [CHECKUP] [IS INDEX] 8153` 检查 passiveobject，行为 0 创建 8023、空 particle、level `40`、随机 pos `70 -70` / `30 -30 0`，行为 1 自毁。`runningicedrop.obj` 挂 `Action/RunningIceDrop.act` 和 5 个 etc action，并有 `[homing]`：use `1`、follow `[ENEMY]`、velocity `100 100`、check gap `300`、sync animation rotation `0`、max rotation `270`；basic action FRAME0 `[SET ACTION] [CUSTOM] 4 [NOW]`，FRAME6 reset trigger 后 `[DESTROY]`。`RunningIceDropStraight/Up/Down/Random/2.act` 均创建 8023；Straight/Up/Down 为 7 帧 level `40` 轨迹，Random 为 6 帧 level `40` 且带 `[USE MY BASEPOS]`，后 3 次还带 `[USE MY DIRECTION]`，RunningIceDrop2 为 7 帧 level `47`。8023 按 passiveobject registry 命中 `ActionObject/SPC/IceDrop.obj`，同号有 stackable 碰撞；`IceDrop.obj` 挂 `Action/IceDrop.act` 与 `AttackInfo/IceDrop.atk`，对象级 on-end-animation 销毁。`.atk` 为 damage `1200`、magic、attack enemy `1`、`[warter element]`、damage reaction damage、push `100`、lift `-100`、hit down、cut、no blood `100 1.0`，不含 hitbox 坐标。`IceDrop.act` BASE `Blizzard_Big.ani`、SUB `Blizzard_Big_Effect.ani`、FRAME1 播放 `IceDrop.ptl`；`Blizzard_Big.ani` 三帧中有 `[ATTACK BOX]`，坐标分别覆盖 `-69 -18 79 68 40 115` 与 `-40 -28 -16 72 60 145`，SUB effect、Dummy、TimeControl、TimeControl2、Kemuri01/02/03 均未观察到盒字段。13 个源对象/action 文件内未观察到 `[SUMMON MONSTER]`；`[homing]` 只命中 `runningicedrop.obj`；`[CREATE PASSIVEOBJECT]` 只命中 `flowermanager.act`、`icedropmanager.act` 与 5 个 running etc action。辅助对照同 14 个 icedrop 命名文件与 8 个 root SPC animation，核心 action/atk/ANI/8023/8153 路由同形；差异为 passiveobject registry 总量/行号不同，8023 在辅助对照额外命中 quest registry，8153 额外命中 map registry，且 `flowermanager.obj` 的 sound category 为 `FIRE_02_LOOP -1`，不能覆盖主目标结论。 |
| `actionobject/SPC/_diredirt` | 主目标该前缀 9 个文件已做小桶只读复核。`body.obj` 与 `body2_weapon.obj` 同构为 width `1 1`、floating height `10`、normal layer、pass all、piercing power `0`、last action `Action/destroy.act`、hp max `800`、hp destroy `1`、after-create sound `ELBON_WIND -1`、对象级 `[object destroy condition] [on end of animation]`，差异为 `body.obj` 挂 `Action/body.act` / `AttackInfo/body.atk` / team `100`，`body2_weapon.obj` 挂 `Action/body2.act` / `AttackInfo/Body2_weapon.atk` / team `99`。两个 basic action 均以 `../animation/Body.ani` 为 BASE ANI，在 `[ON ATTACKSUCCESS]` 时对 `[ME]` 行为 0 `[DESTROY]`，并在 `[WHICH] [LAST ATTACKSUCCESS]` 下对 `[CHECKUP OBJECT] 1` 执行行为 1 `[DISEASE APPENDAGE]`；`body.act` 列形为 `6000 150 200 1000`，`body2.act` 列形为 `100 0 150 1000`。`destroy.act` 仅挂 `../animation/Destroy.ani`。`body.atk` 为 damage `400`、magic、attack enemy `1`、dark element、damage reaction damage、push aside `30`、lift up `100`、hit horizon、blow、no blood `30 0.20000000298023224`；`body2_weapon.atk` 同列形但 damage `8000`，并额外有 `[active status] [poison] 75 70 1500 2200`。`.atk` 不提供 hitbox 坐标。`Body.ani` LOOP `1`、FRAME MAX `4`，FRAME000-003 均有同形 `[ATTACK BOX] -10 -10 -2 20 20 8` 与 `[DAMAGE BOX] -20 -10 -10 40 20 23`；`Destroy.ani` FRAME MAX `5`，仅观察到 `Map/Act6/DireDirt.img` 图像、坐标和 delay，未观察到盒字段。9 个源文件内未观察到 `[CREATE PASSIVEOBJECT]`、`[SUMMON MONSTER]` 或 `[homing]`；`[active status]` 只命中 `body2_weapon.atk`；`[DISEASE APPENDAGE]` 只命中 `body.act` 与 `body2.act`。主目标 `passiveobject/passiveobject.lst` 以 `Dirt` 反向检索未命中，本桶未闭合到数字入口，不能把 `<name_8024>` 当 passiveobject ID。辅助对照同 9 文件、action/atk/ANI/无 create/summon/homing 同形；差异为 owner `[name]` 是直接文本，且 passiveobject registry 总量不同。 |
| `actionobject/SPC/_gligexp` | 主目标该前缀 11 个文件已做小桶只读复核。`body.obj`、`body2.obj`、`body3.obj` 同构为 floating height `0`、normal layer、pass all、piercing power `1000`、对象级 `[object destroy condition] [on end of animation]`；`body.obj` 挂 `Action/Body.act` 与 `AttackInfo/Body.atk`，`body2.obj` 挂 `Action/Body2.act` 与 `AttackInfo/Body2.atk`，`body3.obj` 挂 `Action/Body.act` 与 `AttackInfo/Body3.atk`。`Body.act` BASE `../Animation/Exp.ani`，SUB `../Animation/Floor.ani`，sound `FIRE_BOMB 0`；FRAME1 触发行为 0 一次和行为 1 两次，FRAME2/3 各触发行为 1 两次，FRAME4 触发行为 2；行为 0 为 `[FLASH SCREEN] 150 1500 150 200 0 0 0` 与 `[SHAKING] 3 1500`，行为 1 创建 passiveobject 30550、空 particle、level `0`、三段随机 pos `-200 200` / `-100 100` / `50 100`，行为 2 为 `[DESTROY]`。30550 按 `passiveobject/passiveobject.lst` 命中 `passiveobject/Monster/Spirit/FireExplosion.obj`，全 registry 只命中 passiveobject；raw path 含 `Monster` 不改变 registry。30550 下游对象以 `Animation/FireExplosion.ani` basic motion 直连，挂 `AttackInfo/FireExplosion.atk` 与 4 个 `FireExplosionParticle*.ptl` string data；`FireExplosion.ani` 单帧空图有 `[ATTACK BOX] -65 -40 -50 130 80 100`。`Body2.act` 仅挂 BASE `../Animation/Exp2.ani` 与 sound `FIRE_BOMB 0`，未观察到触发或创建。`Body.atk` 为 damage bonus `180`、magic、no element、weapon damage apply `1`、damage reaction damage、push/lift `100/100`、blow、no blood `50 0.5`；`Body2.atk` 为 damage `3200`、ignore defense、no element、weapon damage apply `1`、damage reaction down、push/lift `100/100`、blow、no blood `50 0.5`；`Body3.atk` 同 `Body2.atk` 但 damage `7000` 且有 `[stuck] -100.0`。`.atk` 不提供 hitbox 坐标。`Exp.ani` FRAME000-004 均有同形超大 `[ATTACK BOX] -1080 -360 -87 2101 700 538`；`Exp2.ani` 单帧空图有同形攻击框；`Floor.ani` 6 帧未观察到盒字段。11 个源文件内 `[CREATE PASSIVEOBJECT]` 只命中 `body.act`，未观察到 `[SUMMON MONSTER]`、`[homing]` 或 `[active status]`。全 PVF 搜 30550 另命中技能、道具和 monster action 等上下文，本桶只记录 `_gligexp -> 30550` 的 passiveobject 创建链，不转入其他主线。辅助对照同 11 文件、action/atk/ANI/30550 路由和无 summon/homing/active status 同形；差异为 owner 与下游 FireExplosion 的 `[name]` 为直接文本，且 passiveobject registry 总量/行号不同。 |

| `actionobject/SPC/_cypherobjectfriend -> 8036 _CypherElec` | 主目标 `_cypherobjectfriend` 前缀 20 个文件已做小桶只读复核。`cypherobject.obj` 与 `cypherobject1.obj` 同构为 width `0 0`、floating height `30`、normal layer、pass all、piercing power `1000`、basic action `Action/start.act`、attack info `AttackInfo/attack.atk`、7 个 etc action；差异为 `cypherobject1.obj` 额外有 `[object destroy condition] [parent dead] [destroy action] [only destroy]`。`start.act` 以 `stay.ani` 开始并随机切到 custom 4-7；`wait.act` 在多帧检查 character 距离 `<= 400` 后可切到 attack，并在 `time + 4000 <= GET TIME` 后切到 fall；`rise.act` 切 wait，`fall.act` 随机切 stay1-3，`stay1/2/3.act` 在 character 距离检查通过后切 wait。`attack.act` FRAME0 设置 outline 与随机 X/Y 速度，FRAME6 创建 passiveobject 8036，创建块为 empty particle、level `45`、pos `-5 0 86`，随后清 outline、速度归零并切 custom 1。8036 位于 `[CREATE PASSIVEOBJECT] [INDEX]` 内，按 `passiveobject/passiveobject.lst` 命中 `ActionObject/SPC/_CypherElec/CypherElec.obj`，名称为“麦瑟的魔雷闪电”。下游 `cypherelec.obj` 挂 `Action/body.act` 与 `AttackInfo/attack.atk`，有 `[homing follow] [ENEMY]`、velocity `250 0`、check gap `100000`、sync animation rotation `0`、max rotation `360`、team `100`、after-create sound `GLARINE_ATK -1`、对象级 `[on end of animation]` 销毁；`body.act` 在攻击成功或 `time + 4000 <= GET TIME` 后销毁，没有继续 create/summon。父 `.atk` 为 physic/no element/down/blow payload；下游 `.atk` 为 magic/no element/damage reaction damage 且 damage bonus `-40`；两者都不提供 hitbox 坐标。父 `attack.ani` 与 `attack_glow.ani` 均为 7 帧且每帧有同形 `[ATTACK BOX] -24 -10 72 40 20 40`；wait/rise/fall/stay 系列 ANI 未观察到盒字段。下游 `body.ani` FRAME001-003 有 `-20 -10 -20 40 20 40`，FRAME004 为 `-26 -10 -31 58 20 61`，FRAME005 为 `-37 -10 -37 78 20 76`，其余帧未观察到盒字段。主目标搜索确认 `_cypherobjectfriend` 只有 `attack.act` 一处 `[CREATE PASSIVEOBJECT]`，两个前缀均未观察到 `[SUMMON MONSTER]` 或 `[active status]`；`_cypherobjectfriend` 未观察到 homing，`_CypherElec` 有 homing 但无继续创建。辅助对照同 20 + 11 文件、8036 registry 目标、action/atk/ANI 盒字段和无 summon/active status 同形；差异为 owner/name 直接文本、registry 总量/行号不同，不能覆盖主目标。 |
| `actionobject/SPC/_cypherobject2 -> 8036 _CypherElec` | 主目标 `_cypherobject2` 前缀 33 个文件已做小桶只读复核。8035 位于 `passiveobject/passiveobject.lst`，命中 `ActionObject/SPC/_CypherObject2/CypherObject.obj`，名称为“麦瑟的凳子”；8036 同 registry 命中 `ActionObject/SPC/_CypherElec/CypherElec.obj`；61328 位于 `[WHICH] [MONSTER] [IS INDEX]` 检查上下文，走 `monster/monster.lst` 命中“魔雷者麦瑟·莫纳亨”，不按 passiveobject 解释。`cypherobject.obj` 为 width `0 0`、floating height `30`、normal layer、pass all、piercing power `1000`，挂 `Action/start.act` 与 `AttackInfo/attack.atk`，7 个 etc action 为 `rise/wait/attack/fall/stay3/stay1/stay2`。`start.act` 以 `stay.ani` 开始并随机切 custom 4-7；`wait.act` 在 FRAME1/3/5/7 检查 61328 距离 `<= 300` 且 HP `> 1 [%]` 后可切 attack，并在 `time + 4500 <= GET TIME` 后切 fall；`stay1/2/3.act` 也检查 61328 后切 wait。`attack.act` FRAME0 设置 outline 与随机 X/Y 速度，FRAME6 创建 passiveobject 8036，创建块为 empty particle、level `45`、pos `-5 0 86`，随后清 outline、速度归零并切 custom 1。上游主目标只读命中两个 8035 创建来源：`bluemarble/cypher/action/comein.act` 与 `cartel/cypher/cypher/action/comein.act`，二者在 FRAME0-4 各三次执行同一行为，创建 8035、empty particle、level `45`、随机 pos `-400 400` / `-150 150 0`。`attack.atk` 为 damage bonus `400`、physic、weapon damage apply `1`、attack enemy `1`、no element、down、push/lift `400/200`、hit wav `STAFF_HIT`、hit horizontal、blow、no blood `200 1.0`；同前缀 `slash.atk` 可读但在 `_cypherobject2` owner/action 文本中未观察到挂接，只能作为相邻未挂接边界。父 `attack.ani` 与 `attack_glow.ani` 7 帧均有 `[ATTACK BOX] -24 -10 72 40 20 40`；wait/rise/fall/stay 系列 ANI 未观察到盒字段。相邻 `attack1/2/realattack1/realattack2/move` action 与 ANI 可读，其中 attack/realattack 侧有 damage/attack box，`move.ani` 有 damage box，weapon sidecar 未观察到盒字段；但这些 action 未出现在 `cypherobject.obj` 的 owner 列表或同前缀脚本文本引用中，不能写成当前 owner-linked hitbox。主目标 `_cypherobject2` 源文件内 `[CREATE PASSIVEOBJECT]` 只命中 `attack.act`，未观察到 `[SUMMON MONSTER]`、`[active status]` 或对象级 homing；下游 8036 `_CypherElec` 有 `[homing follow] [ENEMY]`、body.ani 攻击框和攻击成功/计时销毁，但无继续 create/summon。辅助对照同 33 文件、8035/8036/61328 核心路由、OBJ/ACTION/ATK/ANI 盒字段同形；差异为 name 直接文本、registry 总量/行号不同，且辅助对照额外观察到 `stormofmetastasis/ice_wall/cypher2/action/comein.act` 创建 8035，只能作为目标集差异提示，不能覆盖主目标上游来源结论。 |
| `actionobject/SPC/despair_tower/eltis -> 11101 -> 11100 -> 8498` | 主目标 `eltis` 前缀 23 个文件已做小桶只读复核。11101 和 11100 均走 `passiveobject/passiveobject.lst`，分别命中 `ActionObject/SPC/Despair_tower/Eltis/Eltis_dummy.obj` 与 `Eltis_machine.obj`；10902 位于 `[WHICH] [AI CHARACTER] [IS INDEX]` 上下文，走 `aicharacter/aicharacter.lst` 命中 `gunner/eltis/eltis.aic`；8498 位于 `[CREATE PASSIVEOBJECT] [INDEX]` 上下文，走 passiveobject registry 命中 `ActionObject/Monster/Lizard_man/Event_Thunder_all.obj`，raw path 含 `Monster` 不改变 registry。上游 `aicharacter/gunner/eltis/action/casting.act` 可创建 11101；`eltis_dummy.obj` 为 pass all / hp destroy 管理对象，`eltis_dummy.act` 以 6 个定时触发块创建 11100，pos 为 `290 160 0`、`800 160 0`、`155 333 0`、`944 333 0`、`534 160 0`、`534 333 0`，并在 11100 数量检查或 10902 缺失检查后切 custom 1。`eltis_machine.obj` 为 do not pass / hp destroy 对象，`eltis_machine.act` 检查 11100 数量后切爆炸动作；`eltis_machine_explode.act` 在 FRAME8 检查敌方队伍后创建 8498，empty particle、level `0`、pos `0 0 100`、`[USE NEUTRAL TEAM]`、`[USE OBJECT ZPOS]`，随后 `[APPEND HIT] 0 0 0` 并自毁。`saintdarkexp.atk` 在目录内存在，但主目标 `passiveobject/` 脚本文本未观察到挂接，不写成 owner-linked 攻击信息。`eltis` 源文件内 `[CREATE PASSIVEOBJECT]` 只命中 dummy 和 machine explode 两个 action，未观察到 `[SUMMON MONSTER]`、`[homing]` 或 `[active status]`。`eltis_machine` 系列 ANI 有同形 `[DAMAGE BOX] -25 -11 -5 51 22 94`；dummy 与 destroy particle ANI 未观察到盒字段；8498 下游 `Event_Thunder_all8.ani` FRAME006 有两条 `[ATTACK BOX] -29 -10 0 54 20 443` 与 `-73 -42 229 168 84 215`，其余 10 个 thunder BASE/SUB ANI 当前未观察到盒字段。辅助对照同 23 文件、11101/11100/8498/10902 路由、action 触发链和关键 ANI 盒字段同形；差异为 name 字符串样式、registry 总量/行号不同，只能作为目标集差异提示。 |
| `actionobject/SPC/despair_tower/gravity_d / gravity` | 主目标 `despair_tower` 下 `gravity` 命名文件共 12 个：2 个 `.obj`、5 个 action、5 个 ANI。9288 经 `passiveobject/passiveobject.lst` 命中 `ActionObject/SPC/Despair_tower/gravity_d.obj`，9287 命中 `ActionObject/SPC/Despair_tower/gravity.obj`；二者均不是 monster registry。`gravity_d.obj` 为 width `0 0`、floating height `0`、normal layer、pass all、piercing power `1000`，basic action 为 `Action/gravity.act`，etc action 为 `Action/gravity_stay_d.act`、`Action/gravity_stay1_d.act`、`Action/gravity.act`；普通 `gravity.obj` 同形但 etc action 指向非 `_d` 的 `gravity_stay.act` / `gravity_stay1.act`。`gravity.act` 仅挂 `gravity.ani`，在全角色队伍距离 `<= 5000` 且 checked no `>= 1` 后记录 `timecome1`，4 秒后切 custom 0，并在 FRAME11 设回 FRAME3。`gravity_stay_d.act` / `gravity_stay1_d.act` 分别挂两组或四组 SUB ANI，20 秒或 10 秒后切 custom 1/2；普通 stay 版本额外有 `[CHECKUP OBJECT] 1`、`[FLASH SCREEN EX]` 与 `[APPENDAGE]` 减速/卡肉字段，20 秒版本为 attack/move/cast `-30.0`、stuck `-40.0`，10 秒版本四项均为 `-60.0`。主目标未观察到 `gravity` 命名 attackinfo 文件，也未在 7 个对象/action 源文件中观察到 `[attack info]`、`[CREATE PASSIVEOBJECT]`、`[SUMMON MONSTER]`、`[homing]` 或 `[active status]`。上游入口不是 create 链，而是地图 `[passive object]` 放置块：同一地图块按 `ID X Y Z` 重复列形放置 10387、9288、9288、9288、9287，其中 10387 同样走 passiveobject registry 命中 `ActionObject/Map/towerofdespair/banner_up.obj`。5 个 linked ANI 均可反编译且未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`；本桶是“地图放置 / 表现与减速管理 / 无静态 hitbox 盒字段”的反样本。辅助对照同 12 文件、9288/9287 registry、地图放置列和关键动作/ANI 同形；差异为 passiveobject registry 总量/行号不同，地图名称字符串链接样式不同，只能作为目标集差异提示。 |
| `actionobject/SPC/despair_tower/shouting / shouting2` | 主目标 `despair_tower` 下 `shouting` 命名文件共 12 个：`shouting.obj`、`shouting2.obj`、2 个 action、2 个 `.atk`、6 个 ANI。9290 经 `passiveobject/passiveobject.lst` 命中 `ActionObject/SPC/Despair_tower/shouting.obj`；`shouting2.obj` 物理可读，但当前未闭合到主目标 passiveobject registry 数字入口。`shouting.obj` 与 `shouting2.obj` 均为 floating height `0`、pass all、piercing power `1000`、team `100`，差异为 layer 分别是 `[close]` 与 `[normal]`，basic action / attackinfo 分别指向 `Action/shouting.act` / `AttackInfo/shouting.atk` 与 `Action/shouting2.act` / `AttackInfo/shouting2.atk`。`shouting.act` BASE 为 `shouting.ani`，SUB 为 `shouting1.ani`、`shouting2.ani`，FRAME0/2/4/6 执行 `[ATTACKRECT] [RESET]`，FRAME7 `[DESTROY]`；`shouting2.act` BASE 为 `shouting_1.ani`，SUB 为 `shouting1_1.ani`、`shouting2_1.ani`，FRAME3/6 reset、FRAME7 destroy。`shouting.atk` 含 absolute damage `230`、damage bonus `300`、physic、attack friend `1`、attack enemy `1`、no element、down/blow、no blood `70 0.699999988079071`，并有 `[active status] [stun] 100 70 2000` 与 `[confuse] 100 70 10000 0`；`shouting2.atk` 同核心 payload，但未观察到 attack friend，状态为 `[stun] 50 1 2000` 与 `[curse] 50 1 7000 0 500 0 500`。`shouting.ani` 和 `shouting_1.ani` 的 FRAME000-006 均有 `[ATTACK BOX]`，FRAME007 未观察到盒字段；4 个 SUB ANI 未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`。上游只读命中 `monster/newmonsters/towerofdespair/cross_axe/action/_casting.act` 在 FRAME3 创建 9290，empty particle、level `70`、pos `0 0 50`；同号技能命中只作数字噪声，不写成创建证据。限定检索中，本 6 个对象/action/atk 源文件未观察到 `[CREATE PASSIVEOBJECT]`、`[SUMMON MONSTER]` 或 `[homing]`，`[active status]` 只出现在两个 `.atk`。辅助对照同 12 文件、9290 registry 目标、上游创建入口、action/atk/BASE 攻击框和 SUB 无盒形态；差异为对象 `[name]` 字符串样式与 registry 总量/行号不同，只能作为目标集差异提示。 |
| `actionobject/SPC/despair_tower/shouting2 registry / upstream follow-up` | 主目标反向复核确认：`passiveobject/passiveobject.lst` 未观察到 `shouting2.obj` raw path；全 PVF 脚本文本未观察到 `ActionObject/SPC/Despair_tower/shouting2.obj` 或 `shouting2.obj` 被上游引用；9290 相邻 registry 行与 `_casting.act` 创建入口均只闭合到 `shouting.obj`。`Action/shouting2.act`、`AttackInfo/shouting2.atk` 与 `../Animation/shouting_1.ani` 的同名字符串还命中其他 owner，不能把同名 basename 合并为 `despair_tower/shouting2` 的正式入口证据。辅助对照同样未观察到 `shouting2.obj` registry / 上游入口，只提示对象 `[name]` 字符串样式、registry 总量和行号差异。 |

| `actionobject/SPC/despair_tower/summon_marcelo` | 主目标 `despair_tower` 下 `summon_marcelo` 命名文件共 3 个：`.obj`、1 个 action、1 个 ANI。9291 经 `passiveobject/passiveobject.lst` 命中 `ActionObject/SPC/Despair_tower/summon_marcelo.obj`；对象为 floating height `0`、close layer、pass all、piercing power `1000`，basic action 为 `Action/Summon_marcelo.act`，未观察到对象级 `[attack info]` 或 `.atk`。地图 `towerofdespair_up/15231despair082.map` 的 `[passive object]` 块按 `ID X Y Z` 列形放置 9291，列为 `9291 576 302 0`；同图 `[ai character]` 块放置 20435。`summon_marcelo.act` BASE 为 `../Animation/Summon_marcelo.ani`，触发器 0 默认 ON，检查 `[PLAYER]` 距离 `<= 8000` 与 `[AI CHARACTER] [IS INDEX] 20435` 距离 `<= 8000` 且 checked no `< 1` 后执行行为 0；行为 0 为 `[SUMMON APC] [INDEX] 20435 [LEVEL] 70 [POS] 0 0 20`，并带 `[NO EFFECT]`、`[USE MY DIRECTION]`、`[TEAM] 0`、`[USE OBJECT ZPOS]`，随后 trigger 0 OFF、trigger 1 ON。触发器 1 默认 OFF，检查同一 player/AIC 距离且 checked no `>= 1` 后执行行为 1，把 trigger 0 ON、trigger 1 OFF。20435 在 `[SUMMON APC] [INDEX]` 与 `[WHICH] [AI CHARACTER] [IS INDEX]` 上下文均走 `aicharacter/aicharacter.lst`，命中 `aicharacter/priest/marcelo/marcelo.aic`，不是 passiveobject 或 monster registry。`summon_marcelo.ani` 为 7 帧 `ground-big.img` lineardodge 表现，未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`。本 3 个源文件内未观察到 `[CREATE PASSIVEOBJECT]`、`[SUMMON MONSTER]`、`[homing]` 或 `[active status]`；同号/相邻搜索中 `cross_axe` action 创建 9289，不创建 9291，只作边界提示。辅助对照同 3 文件、9291 registry、地图放置、20435 AIC/APC 路由、action 触发器和 ANI 无盒结论同形；差异为对象与地图名称为直接文本、registry 总量/行号不同，辅助对照额外技能数值命中只能作目标集差异提示。 |
| `actionobject/SPC/despair_tower/booldRust` | 主目标 `despair_tower` 下 `booldRust` 命名文件共 3 个：`.obj`、2 个 action。9289 经 `passiveobject/passiveobject.lst` 命中 `ActionObject/SPC/Despair_tower/booldRust.obj`，全 registry 只命中 passiveobject；对象为 floating height `0`、normal layer、pass all、piercing power `1000`、team `100`，basic action 为 `Action/booldRust.act`，etc action 为 `Action/booldRust_EX.act`，未观察到对象级 `[attack info]` 或 `.atk`。`booldRust.act` BASE 为 `../Animation/GrabBlastBloodEx/cast1.ani`，SUB 为 `cast2.ani`、`cast_light_dodge.ani`、`drain_blood_normal.ani`，FRAME9 执行行为 0 切 custom 0。`booldRust_EX.act` BASE 为 `exp_blood_normal.ani`，SUB 为 `exp_blood_dodge.ani`、`exp_blood_around_dodge.ani`、`exp_light_back_dodge.ani`、`exp_light_dodge.ani`、`exp_light_front_dodge.ani`、`exp_light_particle_dodge.ani`，FRAME9 执行行为 0 `[DESTROY]`。11 个 linked ANI 中，只有 `cast1.ani` 的 FRAME000 观察到 `[ATTACK BOX] -1 -1 810 1 1 2`；`cast1.ani` 其余帧和另外 10 个 linked ANI 未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`。上游只读命中 `monster/newmonsters/towerofdespair/cross_axe/action/casting.act` 在 FRAME10-16 通过 7 个 `[CREATE PASSIVEOBJECT] [INDEX] 9289` 行为创建本对象，empty particle、level `70`、`[USE MAP POS]`，pos 为 `569 170/208/242/287/317/350/380 50`，方向右/左交替。该上游 action 的 FRAME19 还有 `[AI CHARACTER] [IS INDEX] 20435` 检查和 `[ACTIVE STATUS] [bleeding] 100 70 50000 9000` 行为；20435 走 `aicharacter/aicharacter.lst` 命中 `priest/marcelo/marcelo.aic`，不按 passiveobject 或 monster 解释。`skill/thief/moonarc.skl` 的 9289 位于 `[pvp] [level info]` 数值表，是数字噪声，不写成创建证据。限定检索中，本 3 个对象/action 源文件未观察到 `[CREATE PASSIVEOBJECT]`、`[SUMMON MONSTER]`、`[homing]`、`[attack info]`、`[active status]` 或 `[ACTIVE STATUS]`。辅助对照同 3 文件、9289 registry 目标、上游创建 action、20435 AIC 路由、linked ANI 文件名/dataLength 和 `cast1.ani` 攻击框同形；差异为 passiveobject/aicharacter registry 总量和行号不同，辅助对照额外技能数值命中只能作目标集差异提示。 |

| `actionobject/SPC/despair_tower/sogno_hit` | 主目标 `despair_tower` 下 `sogno_hit` 相关命名文件共 12 个，其中 PassiveObject 侧为 `.obj`、1 个 action、1 个 ANI，AIC/AI/key 侧为 `sogno.aic`、4 个 AI 文件、3 个 key 文件。9940 经 `passiveobject/passiveobject.lst` 命中 `ActionObject/SPC/Despair_tower/sogno_hit.obj`，全 registry 只命中 passiveobject；15414 经 `aicharacter/aicharacter.lst` 命中 `magician/sogno/sogno.aic`，主目标全 registry 只命中 aicharacter；23052 经 `passiveobject/passiveobject.lst` 命中 `Character/Mage/MegaDrillReady.obj`，全 registry 只命中 passiveobject。`sogno_hit.obj` 为 floating height `0`、normal layer、pass all、piercing power `1000`，basic action 为 `action/sogno_hit.act`，未观察到对象级 `[attack info]` 或 `.atk`。地图 `towerofdespair_down/15178despair049.map` 的 `[passive object]` 块按 `ID X Y Z` 列形放置 9940，列为 `9940 600 319 0`；同图 `[ai character]` 块放置 15414，列为 `15414 674 287 0 [monster] [boss] 2 0`。`sogno_hit.act` BASE 为 `../Animation/sogno_hit.ani`，FRAME4 检查 `[PASSIVE] [IS INDEX] 23052` 距离 `<= 6000` 且 checked no `< 1`，再检查 `[AI CHARACTER] [IS INDEX] 15414` 距离 `<= 6000` 且 checked no `>= 1`，随后对 `[CHECKUP OBJECT] 0` 执行行为 0；行为 0 为 `[SET ACTION] [STAND] [NOW]`。`sogno_hit.ani` 为 5 帧透明 totem 图像表现，未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`。本 12 个 Sogno 相关源文件内未观察到 `[CREATE PASSIVEOBJECT]`；本 3 个 PassiveObject 源文件未观察到 `[SUMMON MONSTER]`、`[homing]`、`[attack info]`、`[active status]` 或 `[ACTIVE STATUS]`。主目标 9940 全局数字命中 14 条，当前桶只把 registry 和 15178 map 作为本链路证据；技能、道具、replay 等同数字命中不能写成创建证据。辅助对照同 12 个文件、核心 3 文件内容、map 放置、9940 registry、23052 registry、action 检查和 ANI 无盒结论同形；差异为 passiveobject/aicharacter registry 总量与行号不同，23052 文件摘要长度不同，15414 在辅助对照中还同时命中 map 与 passiveobject registry，且 9940/15414 有更多全局数字命中，这些仅作目标集差异提示。 |
| `actionobject/SPC/despair_tower/apc_niddle / apc_niddle2` | 主目标 `despair_tower` 下本桶核心文件为 2 个 `.obj`、2 个 action、2 个 `.atk`、2 个 BASE ANI。9236 经 `passiveobject/passiveobject.lst` 命中 `ActionObject/SPC/Despair_tower/APC_niddle.obj`，9237 命中 `ActionObject/SPC/Despair_tower/APC_niddle2.obj`，主目标全 registry 均只命中 passiveobject。`apc_niddle.obj` 为 normal layer、pass all、piercing power `1000`，basic action `Action/APC_niddle.act`，attack info `attackinfo/APC_pinAttack.atk`，object destroy condition 为 `[time limite] 10000`；`apc_niddle2.obj` 同形但 basic action / etc action 均为 `Action/APC_niddle2_1.act`，attack info 为 `attackinfo/APC_pinAttack1.atk`，同样 10000 时间销毁。`apc_niddle.act` BASE 为 `../animation/APC_ATHiddenStingAttack.ani`，攻击成功后对自身执行行为 0 `[DESTROY]`；`apc_niddle2_1.act` BASE 为 `../animation/niddle2.ani`，攻击成功后自毁，FRAME0 另执行行为 1 `[SET SPEED] [X AXIS] 500 [USE MY DIRECTION]`。`APC_pinAttack.atk` 为 damage bonus `0`、physic、weapon damage apply `1`、no element、damage、push aside `100`、lift up `200`、hit wav `HS_HIT`、hit down、cut、blood `120 1.2000000476837158`，并有 `[active status] [curse] 100 70 20000 0 0 0 250`；`APC_pinAttack1.atk` 同核心 payload，但 blood 为 `120 10.199999809265137`，未观察到 `[active status]`。`APC_ATHiddenStingAttack.ani` 4 帧均有 `[ATTACK BOX] -27 -8 -8 47 16 19`；`niddle2.ani` 6 帧均有 `[ATTACK BOX] -33 -10 -4 67 20 10`；两份 ANI 反编译文本未观察到 `[DAMAGE BOX]`。本 6 个对象/action/atk 源文件内未观察到 `[CREATE PASSIVEOBJECT]`、`[SUMMON MONSTER]` 或 `[homing]`。主目标 9237 的上游创建入口为 `aicharacter/atfighter/roychan/action/niddle_pin.act`，FRAME4 创建 9237，particle `../key/shot.ptl`、level `70`、pos `80 0 55`；9236 当前未闭合到创建入口，技能/字典/replay 命中只作数字噪声。辅助对照核心文件、9236/9237 passiveobject 路由、action/atk/ANI 盒字段同形；差异为 passiveobject registry 总量/行号不同，辅助中 9236/9237 还碰撞 quest registry，9237 额外有 `aicharacter/yitaapc/atfighter/langwang/action/niddle_pin.act` 创建入口，且 9236/9237 全局数字命中更多，这些只能作目标集差异提示。 |
| `actionobject/SPC/despair_tower/apc_niddle 9236 upstream unresolved follow-up` | 主目标全 PVF 数字检索复核 9236：命中范围仅为 itemdictionary、两份 gunner replay、`passiveobject/passiveobject.lst`、`skill/mage/void.skl` 与 `skill/mage/voidex.skl`；skill 命中处是 `[level info]` 或 `[pvp] [level info]` 数值表，不是 `[CREATE PASSIVEOBJECT]`。当前未观察到主目标 action/key/map 创建或放置 9236。辅助对照 9236 仍未观察到 action/key 创建入口，但额外有 quest/dungeon/skill 等数字噪声；辅助 9237 额外有 `yitaapc/.../niddle_pin.act` 创建入口。 |
| `actionobject/SPC/despair_tower/reflect_ball -> eg_bomb` | 主目标本桶核心链路为 `reflect_ball.obj`、3 个 action、`reflect_ball.atk`、5 个 reflect ball ANI、以及下游 `eg_bomb.obj`、`eg_bomb.act`、`eg_bomb.atk`、`eg_bomb2.ani` / `eg-end.ani`。9216 经 `passiveobject/passiveobject.lst` 命中 `ActionObject/SPC/Despair_tower/reflect_ball.obj`，9217 命中 `ActionObject/SPC/Despair_tower/eg_bomb.obj`；二者均按 passiveobject registry 解释。上游 `aicharacter/swordman/ruho/key/attack2.key` 在 `[CREATE PASSIVEOBJECT] [INDEX] 9216` 上下文创建 reflect ball，particle `key/shout.ptl`、level `70`、pos `110 0 100`，并带 `[USE MY BASEPOS]`、`[FIX DIRECTION]`、`[NEUTRAL]`；`destinationselect.ai` 只是检测范围内 passive object 9216，不是创建入口。`reflect_ball.obj` 为 floating height `0`、pass all、piercing power `1000`、basic action `action/reflect_ball.act`、etc action `reflect_ball_stay.act` / `reflect_ball_destroy.act`，attack info 为 `AttackInfo/reflect_ball.atk`。`reflect_ball.act` 和 `reflect_ball_stay.act` 多帧执行 `[ATTACKRECT] [RESET]`，并通过 `[CHECKUP OBJECT] 2` 触发 `[PULL APPENDAGE] 6.5 6.5 500/4500`；`reflect_ball_destroy.act` 在 FRAME2 创建 9217，empty particle、level `70`、pos `0 0 0`、`[USE OBJECT ZPOS]`，FRAME4 自毁。`eg_bomb.obj` 挂 `action/eg_bomb.act` 与 `AttackInfo/eg_bomb.atk`；`eg_bomb.act` BASE 为 `eg_Bomb2.ani`、SUB 为 `eg-end.ani`，FRAME11 自毁，未继续创建下游。`reflect_ball.atk` 为 absolute damage `1200`、physic、no element、damage/cut、push/lift `0`；`eg_bomb.atk` 为 absolute damage `40000`、physic、fire element、down/blow、push aside `500`、lift up `450`；两者未观察到 `[active status]` 或 `[pvp]`。`eg-ball_start.ani` 8 帧均有攻击框，`eg-ball_stay.ani` 40 帧均有同形攻击框，`eg_bomb2.ani` 的 FRAME003-006 有攻击框；`eg-ball-normal.ani`、`eg-ball-end.ani`、`eg-ball-end-normal.ani`、`eg-end.ani` 未观察到攻击框或伤害框，本桶 linked ANI 均未观察到 `[DAMAGE BOX]`。本桶对象/action 源文件未观察到 `[SUMMON MONSTER]`、`[SUMMON APC]`、`[homing]`；技能表 9216/9217 命中只作数字噪声。辅助对照核心文件、9216/9217 passiveobject 路由、创建入口、action/atk/ANI 盒字段同形；差异为辅助对照 9216 还碰撞 stackable registry，9216/9217 全局数字命中更多，且 registry 总量/行号不同，这些只作目标集差异提示。 |

| `actionobject/SPC/despair_tower/attack_shield / pass_shield -> attack_shield_eff` | 主目标本桶核心链路为 `attack_shield.obj`、`pass_shield.obj`、二者 action/atk/BASE ANI、以及下游 9219 `Attack_shield_eff.obj` / `Attack_shield_eff.act` / `Attack_shield_eff.ani`。9218 经 `passiveobject/passiveobject.lst` 命中 `ActionObject/SPC/Despair_tower/pass_shield.obj`，9222 命中 `Attack_shield.obj`，9219 命中 `Attack_shield_eff.obj`；三者均按 passiveobject registry 解释。上游 `aicharacter/priest/yeomyung/key/attack38.key` 创建 9218 五次，`attack38_1.key` 创建 9218 三次，`attack106.key` 创建 9222 一次；这些是 AIC key 边界，不转入 APC/AI 主线。`attack_shield.obj` 挂 `Action/Attack_shield.act` 与 `AttackInfo/Attack.atk`，`pass_shield.obj` 挂 `Action/pass_shield.act` 与 `AttackInfo/hit.atk`，并有 `[hp destroy] 1` 和 destroyobject 声音。`attack_shield.act` 在攻击成功上下文创建 9219，empty particle、level `70`、pos `0 0 0`、`[USE OBJECT ZPOS]`，FRAME10 自毁；`pass_shield.act` FRAME0 反向并关闭 trigger，FRAME8 创建 9219，empty particle、level `70`、pos `-10 1 0`、`[USE OBJECT ZPOS]`，随后自毁。9219 effect 对象不挂 attackinfo，`attack_shield_eff.act` FRAME5 自毁。`Attack.atk` 为 absolute damage `25000`、physic、no element、damage、push/lift `900/300`、blow；`hit.atk` 为 absolute damage `8000`、physic、no element、damage、knuck back `-1`、blow；两者未观察到 `[active status]` 或 `[pvp]`，且 `.atk` 不提供 hitbox 坐标。`attack_shield.ani` FRAME004-007 有攻击框，`pass_shield.ani` FRAME003-007 有攻击框；`attack_shield_eff.ani` 与 `pass_shield1.ani` 未观察到攻击框或伤害框，本桶 linked ANI 均未观察到 `[DAMAGE BOX]`。本桶对象/action 源文件未观察到 `[SUMMON MONSTER]`、`[SUMMON APC]`、`[homing]`。主目标另观察到 `defence_shield.act` 与 `trap_defence_shield_stay.act` 也创建 9219，这是 9219 的相邻外部入边，不能把 defence/trap owner 当成本桶已展开完成。辅助对照核心文件、9218/9219/9222 registry raw path、上游创建、action/atk/ANI 盒字段同形；差异为对象 name 字符串样式、文件长度、registry 总量/行号以及更多技能/回放数字命中，这些只作目标集差异提示。 |

| `actionobject/SPC/despair_tower/star / defence / trap defence shield` | 主目标本桶核心链路为 `Star_shield.obj -> M_defence_shield_bottom.obj -> M_defence_shield.obj -> Attack_shield_eff.obj` 与 `defence_shield_bottom.obj -> defence_shield.obj -> Attack_shield_eff.obj`，并记录 `center_shield` / `shield_count` 管理边界。9220/9221/9263/9264/9265/9266/9267 均经 `passiveobject/passiveobject.lst` 命中同路径 shield 对象；20429 位于 AI CHARACTER 检查上下文，走 `aicharacter/aicharacter.lst` 命中 `priest/yeomyung/yeomyung.aic`。上游 `attack70.key` 创建 9221，`attack70_1.key` 创建 9265；`action_main.ai` 会用 9220 passive object 数量检查选择 attack70。`defence_shield_bottom.act` FRAME4 创建 9220，FRAME6 对角色距离 `<= 115` 的检查对象执行 `[MUCU LIMIT CONTROL] 7000`，FRAME12 自毁；`defence_shield.act` ON DAMAGE 创建 9219 并 shaking，另在 FRAME11 检查 9221 后对 checkup object 销毁并多次创建 9219。`star_shield.act` FRAME5 按地图坐标创建 9264 三处，FRAME7 创建 9266，FRAME8 自毁；`trap_defence_shield_bottom.act` FRAME4 创建 9263，FRAME12 切 custom 0；`trap_defence_shield_stay.act` ON SET ACTION 初始化 `Dcount`，ON DAMAGE 增计数并创建 9219，`Dcount >= 10` 时创建 9219 后自毁，也会在 20429 AIC 不足时自毁。`trap_defence_shield_bottom_stay.act` 在 20429 缺失或附近 9263 缺失时自毁。`center_shield.act` 对 20429 checkup object 添加防御/回血/抗性/stuck/hit recovery appendage；`center_shield_stay.act` 检查 9264 与 20429 后创建 9267、删除 appendage 并自毁；`shield_count.act` 检查 20429 HP 与 9265 存在性后自毁。17 个对象/action 源文件内 `[CREATE PASSIVEOBJECT]` 只命中 6 个 action，未观察到 `[SUMMON MONSTER]`、`[SUMMON APC]`、`[homing]`、`[attack info]`、`[active status]` 或 `[pvp]`。本桶无 `.atk` owner；hitbox/碰撞盒只来自 linked ANI：`defence_shieldme.ani` FRAME004-011 有 `[DAMAGE BOX]`，`trap_defence1_shield.ani` FRAME004-011 有 `[DAMAGE BOX]`，`trap_defence1_shield_stay.ani` FRAME000-002 有 `[DAMAGE BOX]`；`defence_shield_tailme.ani`、`trap_defence_shield_losttail/lostmagic*.ani`、`shield_diamond*.ani`、`star_shield.ani`、`conter_shield*.ani`、`shield_count.ani` 未观察到攻击框或伤害框。本桶 linked ANI 未观察到 `[ATTACK BOX]`，不能写成攻击输出链；9219 effect 的具体表现沿用上一桶已复核结论。辅助对照同 43 个 shield 命名文件、核心 registry raw path、action/key/ANI 盒字段同形；差异为 passiveobject/aicharacter registry 总量与行号、对象脚本长度和 name 字符串样式不同，只能作目标集差异提示。 |
| `actionobject/SPC/despair_tower/defence_shield_eff / defence_shield_magic2 sidecar boundary` | 主目标两个 ANI 物理存在且可反编译，但全 PVF 脚本文本未观察到文件名或不带扩展名 token 引用；两者均未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`。辅助对照同路径、dataLength、无引用、无盒同形。本行只记录未挂接侧车边界，不能写成 owner-linked hitbox 或运行播放结论。 |

| `actionobject/monster/new_event/Hgoblin_Laser` | 主目标 16157 经 `passiveobject/passiveobject.lst` 命中 `ActionObject/Monster/New_Event/Hgoblin_Laser.obj`；同号另有 equipment registry 碰撞，本桶对象 ID 和创建块均按 passiveobject registry 解释。`Hgoblin_Laser.obj` 为 floating height `1`、normal layer、pass all、piercing power `1000`，basic action `Action/Hgoblin_laser.act`，attack info `AttackInfo/BiggunEx.atk`，`[name]` 为字符串链接样；对象本体未观察到 width、team、对象级 destroy condition 或 `[homing]`。`Hgoblin_laser.act` BASE `laser_dodge.ani`，SUB `laser_dodge1.ani 0 -5` 与 `laser_dodge2.ani 0 -4`，播放 `MECAGOBLIN_LAZER`，FRAME12 对自身执行行为 0，行为 0 为 `[DESTROY]`；action 内未观察到 `[ATTACKRECT] [RESET]`。`BiggunEx.atk` 为 damage `600`、magic、light element、damage reaction damage、push/lift `30/30`、hit wav `THUNDERC_ELEC_HIT`、blow、no blood `50 0.5`，不提供 hitbox 坐标。`laser_dodge.ani` 为 13 帧，FRAME001-006 均为同形 `[ATTACK BOX] -7 -10 -10 660 20 21`，FRAME000/007-012 未观察到盒字段；SUB `laser_dodge1.ani` 与 `laser_dodge2.ani` 均为 13 帧表现 ANI，未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`。主目标上游创建闭合两个入口：`zx_77/action/attack.act` 与 `attack1.act` 在 FRAME2 创建 16157，创建块为空 particle、level `1`、POS `15 0 1`、`USE OBJECT ZPOS`、`COLLISION GROUP 2 0 1`，二者差异为 FRAME0 分别设置 LEFT/RIGHT。限定源文件内未观察到继续 `[CREATE PASSIVEOBJECT]`、`[SUMMON MONSTER]`、对象级 `[homing]`、`.atk [active status]`、`.atk [pvp]` 或 `[ATTACKRECT]`。辅助对照同 16157 passiveobject 路由、equipment 碰撞、核心 obj/action/atk/linked ANI 同形；差异为对象 `[name]` 和 source action `[SPEECH]` 是直接文本、registry 总量/行号、对象与 source action 脚本长度不同，且辅助额外观察到 `timespiral/zx_77/action/attack.act` / `attack1.act`、`iteminfo.dat` 与 atpriest skill 数字命中，只能作为目标集差异提示，不能覆盖主目标入边结论。 |
| `actionobject/monster/new_event/Boss_Command` | 主目标 16167 经 `passiveobject/passiveobject.lst` 命中 `ActionObject/Monster/New_Event/Boss_Command.obj`；同号另有 equipment registry 碰撞，本桶对象 ID、地图放置和 passive 检查上下文均按 passiveobject registry 解释。`Boss_Command.obj` 为 `[name]` 字符串链接样、floating height `1`、bottom layer、pass all、piercing power `1000`，basic action `Action/Boss_Command.act`，etc action 为 `Action/Boss_Command_Change.act`、`Action/Boss_Command_Change1.act`、`Action/Boss_Command_Attack.act`、`Action/Boss_Command.act`；对象本体未观察到 width、team、对象级 destroy condition、`[homing]` 或 `[attack info]`。四个 linked action 均不挂 `.atk`：`Boss_Command.act` 以 `Boss_Command_test.ani` 为 BASE，并按 `[WHICH] [MONSTER] [IS INDEX] 61644 / 61645` 检查后切 custom 0/1；`Boss_Command_Attack.act` 检查 61644/61645 与 61649/61653，行为 0 含 `[SET DAMAGE BOX] [ON]` 后切 attack 4，行为 1 切 attack 2，后续 custom trigger 开关和 custom 3 切换；`Boss_Command_Change.act` / `Boss_Command_Change1.act` 分别挂 `Boss_Command_test2.ani` / `Boss_Command_test3.ani`，并分别检查 61645 / 61644 后切 attack 3。61644/61645/61649/61653 位于 `[WHICH] [MONSTER] [IS INDEX]` 上下文，走 `monster/monster.lst`，不是 passiveobject ID。三个 linked BASE ANI `Boss_Command_test.ani`、`Boss_Command_test2.ani`、`Boss_Command_test3.ani` 均未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`；相邻物理 `Boss_Command.ani` 可反编译，含 `[DAMAGE TYPE] UNBREAKABLE`，但全 PVF 脚本文本未观察到 `Boss_Command.ani` 文件名引用，且同样未观察到攻击框或伤害框。主目标未观察到 `[CREATE PASSIVEOBJECT]` 创建 16167；`new_eventmap2` 与 `twin_raid_boss` 地图的 `[passive object]` 块静态放置 16167，列形为 `ID X Y Z`；`dual_face` / `dual_face2` 相关 change_start action 只是 `[WHICH] [PASSIVE] [IS INDEX] 16167` 检查并切 custom，不是创建入口。相关 source action 中另有 `[CREATE PASSIVEOBJECT] [INDEX] 16191`，16191 回 passiveobject registry 命中 `ActionObject/Monster/New_Event/EMP.obj`，EMP 链已在独立 EMP 行展开；在 Boss_Command 行中仍只作为相邻入边记录。限定源文件内未观察到 `[SUMMON MONSTER]`、对象级 `[homing]`、`.atk [active status]` 或 `.atk [pvp]`。辅助对照同 16167 passiveobject 路由、equipment 碰撞、9 个核心文件、obj/action/ANI 核心列形和无盒观察同形；差异为对象 `[name]` 为直接文本、registry 总量/行号不同，辅助全局数字命中多出升级表与技能数值噪声，且辅助对照当前搜索只观察到 `new_eventmap2` 地图放置和 `dual_face` 四个 passive 检查入口，未观察到主目标中的 `twin_raid_boss` 放置和 `dual_face2` 两个 passive 检查入口，这些只能作为目标集差异提示。 |
| `actionobject/monster/new_event/Zx-69_Dorp / Zx-69_Dorp1` | 主目标 16161 经 `passiveobject/passiveobject.lst` 命中 `ActionObject/Monster/New_Event/Zx-69_Dorp.obj`，16181 命中 `ActionObject/Monster/New_Event/Zx-69_Dorp1.obj`；16161 同号另有 equipment registry 碰撞，当前对象 ID 与创建块均按 passiveobject registry 解释。两个对象同形：floating height `1`、normal layer、pass all、piercing power `50000`、basic action `Action/Zx-69_Dorp.act`、`[vanish on move collision] 0`；差异为 etc action 分别指向 `Action/Zx-69_Dorp_last.act` 与 `Action/Zx-69_Dorp_last1.act`。对象本体未观察到 width、team、对象级 destroy condition、`[homing]` 或 `[attack info]`。`Zx-69_Dorp.act` BASE 为 `Zx-69-Body_Stay.ani`，SUB 为 `G_main_s_1/2/3.ani`，当自身 ZPOS `<= 10` 且 checked no `>= 1` 时切 custom 0。两个 last action 均在 FRAME28 执行 `[SUMMON MONSTER]` 后 `[DESTROY]`：`last` 召唤 61650，`last1` 召唤 61656；这两个 ID 位于 `[SUMMON MONSTER] [INDEX]` 上下文，走 `monster/monster.lst`，均命中 `Zx-18 机器人` 相关条目，不按 passiveobject 解释。7 个 linked ANI 均未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`；本桶无 `.atk` owner。主目标上游创建闭合：`dual_face/p1_zx_69_summon.act` 三次创建 16161，`dual_face/p2_zx_69_summon.act` 三次创建 16181，`dual_face2/zx_69_summon_l.act` 与 `zx_69_summon_r.act` 各三次创建 16161；创建块均含 particle、level、随机 POS、warning mark、use map pos 与 collision group。`skill/priest/awakening.skl` 中的 16161 只是技能数值表，不是创建入口。限定源文件内未观察到继续 `[CREATE PASSIVEOBJECT]`、`[homing]`、`[attack info]`、`[active status]`、`[pvp]` 或 `[ATTACKRECT]`。辅助对照同 8 个核心文件、16161/16181 passiveobject 路由、61650/61656 summon 路由、obj/action/ANI 无盒观察同形；差异为 registry 总量/行号不同，辅助 16161 只观察到 `dual_face/p1_zx_69_summon.act` 创建入口，未观察到主目标 `dual_face2` 左右两个 16161 入边，且辅助有更多 skill/item 数字噪声，只能作为目标集差异提示。 |
| `actionobject/monster/new_event/ExpAir` | 主目标 16156 经 `passiveobject/passiveobject.lst` 命中 `ActionObject/Monster/New_Event/ExpAir.obj`；同号另有 equipment registry 碰撞，本桶对象 ID 按 passiveobject registry 解释。`ExpAir.obj` 为 `[name]` 字符串链接样、floating height `1`、normal layer、pass all、piercing power `1000`，basic action `Action/ExpAir.act`，attack info `AttackInfo/TempesterMissileExp.atk`；对象本体未观察到 width、team、对象级 destroy condition 或 `[homing]`。`ExpAir.act` BASE `../Animation/Tempester/ExpAirDodge.ani`，SUB `../Animation/Tempester/ExpAirNormal.ani 0 0`，FRAME7 对自身执行 `[DESTROY]`；action 内未观察到 `[CREATE PASSIVEOBJECT]`、`[SUMMON MONSTER]` 或 `[ATTACKRECT]`。`TempesterMissileExp.atk` 为 damage bonus `0`、weapon damage apply `1`、magic、attack enemy `1`、no element、damage reaction down、push/lift `100/200`、hit wav `FIRE_PILLAR_HIT`、blow、no blood `20 1.0`，未观察到 `[active status]` 或 `[pvp]`，且 `.atk` 不提供 hitbox 坐标。`ExpAirDodge.ani` 为 8 帧，FRAME000-004 每帧均有 3 条 `[ATTACK BOX]`：`-45 -30 -55 90 60 110`、`-75 -15 -18 150 30 35`、`-60 -23 -35 120 45 70`；FRAME005-007 未观察到盒字段。`ExpAirNormal.ani` 为 8 帧表现 ANI，未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`。主目标全局数字检索未观察到 16156 的 `[CREATE PASSIVEOBJECT]` 创建入口，只命中 registry、equipment、itemdictionary、event 和 skill 数值噪声；同名 `ExpAir.act` 与 `TempesterMissileExp.atk` 在 common / character/gunner 等前缀另有物理文件，不能与本桶 new_event 物理链合并。辅助对照同 24 个 exp 命名文件、16156 passiveobject 路由、core action/atk/ANI 盒字段同形；差异为对象 `[name]` 为直接文本、registry 总量/行号、对象 dataLength、全局数字噪声集合不同，且辅助额外观察到 `iteminfo.dat` 命中，只能作为目标集差异提示。 |
| `actionobject/monster/new_event/G_main / G_main_s / G_main1` | 主目标 16158/16159/16160 经 `passiveobject/passiveobject.lst` 分别命中 `ActionObject/Monster/New_Event/G_main.obj`、`G_main_s.obj`、`G_main1.obj`；三者同号均有 equipment registry 碰撞，当前对象 ID 只按 passiveobject registry 解释。三个对象均为 width `0 0`、floating height `1`、pass all、piercing power `50000`、hp max `1`、hp destroy `1`，差异为 basic action 分别指向 `Action/G_main.act`、`Action/G_main_s.act`、`Action/G_main1.act`，last action 分别为 `Action/G_Last.act`、`Action/G_Last_s.act`、`Action/G_Last.act`；对象本体未观察到 `[attack info]`、`[homing]`、team 或 layer。`G_main.act` / `G_main1.act` 分别挂 `G_main_0.ani` BASE 和 `G_main_1/2/3.ani` SUB，在 `[WHICH] [MONSTER] [CHECKUP] [IS INDEX]` 61644 / 61645 且 checked no `< 1` 时 `[DESTROY]`；`G_main_s.act` 挂 `G_main_s_0.ani` BASE 和 `G_main_s_1/2/3.ani` SUB，FRAME4 检查 64007/64010/64013 且 checked no `<= 0` 时 `[DESTROY]`。61644/61645/64007/64010/64013 均按 `monster/monster.lst` 解释，不按 passiveobject 解释。linked `G_main_0/1/2/3.ani`、`G_main_s_0/1/2/3.ani`、`G_Last.ani`、`G_Last_s.ani` 均未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`；同前缀 `G_main_4.ani` / `G_main_s_4.ani` 可读且无盒，但脚本文本反查未观察到 action 挂接，只登记为相邻侧车。主目标限定源文件内未观察到 `[CREATE PASSIVEOBJECT]`、`[SUMMON MONSTER]`、`[attack info]`、`[homing]`、`[active status]`、`[pvp]` 或 `[ATTACKRECT]`；全局数字检索未观察到 16158/16159/16160 的 `[CREATE PASSIVEOBJECT]` 创建入口，只命中 registry、equipment、item/event/skill 等数值噪声。主目标另有 `timegate/TimeLord/G_main.obj` 与 `G_main_s.obj`，分别是 15056/15057 的独立 passiveobject registry 入口，不能与本桶合并。辅助对照同 16 个本桶核心文件、16158/16159/16160 passiveobject 路由、obj/action/ANI 无盒和无 create/attackinfo/homing 观察同形；差异为 registry 总量/行号、64007/64010 的显示名称、数字噪声集合不同，且辅助同名 `g_main` 文件明显更多，这些只能作为目标集差异提示。 |
| `actionobject/monster/new_event/Stingerex` | 主目标 16162 经 `passiveobject/passiveobject.lst` 命中 `ActionObject/Monster/New_Event/Stingerex.obj`；同号另有 equipment registry 碰撞，本桶对象 ID 和创建块均按 passiveobject registry 解释。`Stingerex.obj` 为 `[name]` 字符串链接样、floating height `1`、normal layer、pass all、piercing power `1000`、team `0`，basic action `Action/stingerex.act`，attack info `AttackInfo/Stinger.atk`，对象级 destroy condition 为 `[on end of animation]`；对象本体未观察到 width 或 `[homing]`。`stingerex.act` BASE `../Animation/Stinger/ExpDodge.ani`，SUB `../Animation/Stinger/ExpFloorNormal.ani 0 1` 与 `../Animation/Stinger/ExpFloorDodge.ani 0 2`，播放 `BOMB_02`，action 内未观察到 trigger、behavior、`[CREATE PASSIVEOBJECT]`、`[SUMMON MONSTER]` 或 `[ATTACKRECT]`。`Stinger.atk` 为 damage `1000`、physic、weapon damage apply `1`、attack enemy `1`、no element、down、push/lift `100/300`、hit wav `STAFF_HIT`、hit down、blow、no blood `200 1.0`，未观察到 `[active status]` 或 `[pvp]`，且 `.atk` 不提供 hitbox 坐标。`ExpDodge.ani` 为 14 帧，FRAME000-006 均有 `[ATTACK BOX]`，坐标依次为 `-36 -18 0 71 36 28`、`-56 -23 0 110 46 89`、`-65 -23 0 131 46 132`、`-75 -23 0 150 46 148`、`-75 -23 0 150 46 149`、`-75 -23 0 150 46 150`、`-75 -23 0 150 46 149`；FRAME007-013 未观察到盒字段，未观察到 `[DAMAGE BOX]`。`ExpFloorNormal.ani` 与 `ExpFloorDodge.ani` 均为 6 帧表现/地板残留 ANI，未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`。主目标上游创建闭合：`tank_landrunner/action/boom2.act` 在 FRAME5 创建 16162，创建块为空 particle filename、level `80`、pos `50 0 5`，同一 source action FRAME0 设置 X/Z 速度 `100/50`，FRAME6 自毁。主目标另有 `timegate/conflagration/Stingerex.obj` 11178 与 `AdvanceAltar/Stingerex.obj` 12587 等独立 passiveobject 入口，且 `AttackInfo/Stinger.atk` 字符串被多个 owner 复用，不能与本桶 new_event 物理链合并。辅助对照同 6 个本桶核心文件、16162 passiveobject 路由、core action/atk/ANI 盒字段和 new_event `tank_landrunner` 创建入口同形；差异为对象 `[name]` 为直接文本、registry 总量/行号、对象 dataLength、数字噪声集合不同，辅助额外观察到 `timespiral/tank_landrunner/action/boom2.act` 创建 16162，并有更多同名 Stingerex/AttackInfo 复用 owner，这些只能作为目标集差异提示。 |

| `actionobject/monster/new_event/EMP` | 主目标 16191 经 `passiveobject/passiveobject.lst` 命中 `ActionObject/Monster/New_Event/EMP.obj`；当前跨 registry 解析未观察到同号 equipment 碰撞，本桶对象 ID 和创建块均按 passiveobject registry 解释。`EMP.obj` 为 `[name]` 字符串链接样、floating height `1`、normal layer、pass all、piercing power `1000`，basic action `Action/EMP.act`；对象本体未观察到 width、team、对象级 destroy condition、`[homing]` 或 `[attack info]`，因此本桶无 `.atk` owner。`EMP.act` BASE `../Animation/EMP/spin_left_back_dodge.ani`，SUB 依次挂 `emp_nova3.ani 0 -1`、`dis_light_back_dodge.ani 0 1`、`light_normal.ani 0 2`、`line_dodge2.ani 0 3`、`light_dodge.ani 0 4`、`spin_right_back_dodge.ani 0 5`、`emp_nova4.ani 0 6`、`_emp_nova3.ani 0 7`、`_emp_nova4.ani 0 8`。FRAME8 与 FRAME12 均检查 `[WHICH] [CHARACTER] [CHECKUP] [DISTANCE] <= 1000`，checked no `>= 1` 时对 checkup object 执行行为 0；行为 0 为 action-level `[ACTIVE STATUS] [lightning] 100 80 5000 1`，随后 `[SET ACTION] [DOWN] [MY DIRECTION] 1 [FORCE] 100 80 [NOW]`；FRAME23 执行行为 1 `[DESTROY]`。action 内未观察到 `[CREATE PASSIVEOBJECT]`、`[SUMMON MONSTER]` 或 `[ATTACKRECT]`。主目标 owner-linked 10 个 EMP ANI 均已反编译或限定复核，未观察到 `[ATTACK BOX]` 或 `[DAMAGE BOX]`；这些 ANI 只提供图像、位置、倍率、RGBA、lineardodge、flip、delay 等表现字段。主目标上游创建闭合：`dual_face/action/l1_chang_start.act`、`l2_chang_start.act`、`p1_chang_start.act`、`p2_chang_start.act` 均在 FRAME0 创建 16191，创建块为空 particle filename、level `1`、pos `798 463 90`、`[USE MAP POS]`；同组动作另在 FRAME1 检查 16167，16167 回 passiveobject registry 命中 `Boss_Command.obj`，`p1/p2` 还在 FRAME7 检查 61656/61650，二者走 `monster/monster.lst`，只作为相邻上下文记录。主目标同前缀 `tempester/ExpAir*`、`AttackInfo/TempesterMissileExp.atk` 以及 `twin_item/emp_*` 只是文件名包含 `emp` 或相邻资源，未被 `EMP.act` 挂接，不能并入本桶。辅助对照同 16191 registry 路由、21 个同前缀 emp 命名文件、核心 action 列形和 no-box 搜索结果；差异为 registry 总量/行号、`EMP.obj` dataLength 与 `[name]` 直接文本不同，辅助全局 16191 数字命中额外出现两个 skill level-info 数值表，且 `Action/EMP.act` 字符串额外命中 `castleofthedead/metalgear/emp/emp.obj` 的相对路径字符串碰撞，这些只能作为目标集差异提示。 |

| `actionobject/monster/new_event/missile_L / missile_R / missile_EXP / JeffMissileEXP1` | 主目标 16163/16166/16164/16165 经 `passiveobject/passiveobject.lst` 分别命中 `ActionObject/Monster/New_Event/missile_L.obj`、`missile_R.obj`、`missile_EXP.obj`、`JeffMissileEXP1.obj`；四个 ID 均有 equipment registry 碰撞，本桶对象 ID 与 `[CREATE PASSIVEOBJECT] [INDEX]` 均按 passiveobject registry 解释。`missile_L.obj` / `missile_R.obj` 同形，均为 name string-link、floating height `1`、normal layer、pass all、piercing power `1000`，basic action `Action/missile_start.act`，etc action 分别为 `Action/missile_stay.act` / `Action/missile_stay1.act`，对象本体未观察到 `.atk`、team、homing 或 destroy condition。`missile_start.act` 挂 `missile.ani` 与 `missile_eff.ani`，分阶段设置 X/Z 速度并在 FRAME46 切 custom 0；`missile_stay.act` / `missile_stay1.act` 挂 `missileT.ani`，先升空、再 teleport 到不同随机 X 区间，FRAME47 创建 16164 `missile_EXP.obj` 后销毁，左侧版本 FRAME29 另有限次行为触发。`missile_EXP.obj` 挂 `Action/missile_Destroy.act`，该 action 在 ZPOS `<= 30` 后创建 16165 `JeffMissileEXP1.obj` 并销毁；创建块为空 particle、level `80`、pos `0 0 2`、使用自身方向与对象 ZPOS。`JeffMissileEXP1.obj` 为 floating height `0`、normal layer、pass all、piercing power `1000`、team `100`，挂 `Action/JeffMissileEXP.act` 与 `AttackInfo/JeffMissileEXP1.atk`，销毁条件为 time limit `1000`。`JeffMissileEXP1.atk` 为 damage `4000`、absolute damage `3000`、physic、fire、down、push/lift `100/100`、hit lift up、blow、no blood `100 1.0`，未观察到 `.atk [active status]` 或 `.atk [pvp]`，且 `.atk` 不提供 hitbox 坐标。`JeffMissileEXP.act` FRAME0 创建 35112 `ActionObject/Map/Risk_Dungeon/Cartelcommand/LargeExp.obj` 和 35113 `.../sniper_exp.obj`；二者在创建上下文仍走 passiveobject registry，35112 下游 `ShootingStarExp.ani` 单帧有 `[ATTACK BOX] -100 -30 -20 200 60 40`，35113 `sniper_exp.ani` 未观察到盒字段。`JeffMissileEXP.act` 的 `[LAST ATTACKSUCCESS] [IS INDEX]` 61772/61776/61777/61779 均按 `monster/monster.lst` 解释，只用于 checkup object 分支，不是 passiveobject 下游。主目标 linked ANI 分层闭合：`missile.ani`、`missileT.ani`、`missileD.ani`、`missile_eff.ani`、`missile_effD.ani` 均未观察到攻击框或伤害框；`JeffMissileEXP.ani` FRAME000 有 2 条攻击框、FRAME001-003 各有 3 条攻击框，FRAME004-005 无盒，三个 `JeffMissileEXP1/2/3.ani` SUB 未观察到盒字段。主目标上游创建闭合：`dual_face/l2_missile_l.act` 与 `dual_face2/missile_l.act` 创建 16163，`dual_face/l1_missile_r.act` 与 `dual_face2/missile_r.act` 创建 16166；辅助对照同四个核心 ID、obj/action/atk/ANI 和 35112/35113 下游核心列形同形，但 registry 总量/行号、name 文本样式、脚本长度不同，且辅助未观察到主目标 `dual_face2` 左右 missile 入边，并有更多 item/skill/quest 数字噪声，这些只能作为目标集差异提示。 |

## 辅助对照提示

辅助对照中 `passiveobject/` 文件总量、`actionobject/`、`character/`、`.ani`、`.act`、`.atk` 等规模均明显大于主目标。它只能说明目标集差异很大，不能把辅助对照独有字段、ID 或路径写成主目标必然存在。

## 未闭合清单

| 优先级 | 未闭合块 | 下一步只读测试 |
| --- | --- | --- |
| P0 | ANI hitbox 覆盖 | 已有 `[ATTACK BOX]`、linked `[DAMAGE BOX]`、common BASE ANI、mapobject `.obj` 直连 ANI、actionobject BASE/SUB ANI、actionobject/monster 3headlessknight、TimeLord、Blood_An、Grim_seeker、advancealtar arrow 分级 `.atk` + shared action 样本、at_5t_walker rocket 子链、fire/bullet/bombingmissile/exp 小桶、special 批量投放小桶与 barr damage-box/blocking 小桶样本、actionobject/common bigboom 样本、actionobject/SPC 4season、cannontrap、Jackie、sjar、despair_tower/eltis、Agaress_Hell、_airsword 无盒且相邻 ANI 未挂接反样本、_bal BASE 攻击框与 SUB 无盒分层样本、_bat homing 攻击框与粒子侧车无盒样本、_burnfire homing + active status + 两帧同盒样本、_aganzo 无 owner `.obj` 但 action-linked ANI 有盒的未闭合风险样本、_darkcough `.obj [basic motion]` 直连 ANI 攻击框样本、_cypherelec 三 owner homing + action-linked 双 ANI 攻击框样本、_cyclone homing owner 11 帧多攻击框 + opt 空图创建下游样本、_avengers、_cursequeen/void、_cursequeen/spire、_cursequeen/cry、_firewitch/ador、_firewitch/firebreath、_firewitch/meteo、rina 与 mesteria 样本；eltis 补充 machine damage-box、dummy/particle 无盒、8498 下游 thunder 攻击框样本；rina 补充雷击多攻击框、rocketblast BASE/SUB 攻击框、rosiclockball 全帧攻击框、skypotel 空图无盒和粒子侧车断链样本；laowu 补充 owner-linked buff BASE/SUB/SUB ANI WITH XYZ 全无盒样本、owner 未命中的 `empty01.act` balloon 无盒侧车样本，以及相邻可读但未挂接 ANI 反样本；mesteria 补充 source BASE 攻击框、source SUB 无盒、粒子侧车无盒、40005 下游 common 空图攻击框，以及 `HalloweenBusterSub.*` 可读但未挂 mesteria owner 的边界样本；character common/fighter/gunner `.obj` 直连与 action 创建链样本、character SUB ANI 反样本和引用断链样本；下一步继续扩大 actionobject/monster 其他子桶、actionobject/SPC 其他子桶、character 分桶并记录反编译失败样本。 |
| P0 | `[CREATE PASSIVEOBJECT]` 递归链 | 已有双对象回环、Crochan 回环、fan-out、pirateship 多层创建、TimeLord Chain 单步创建、Blood_An 随机候选到攻击成功下游链、Grim_seeker 随机候选与静态可回返分支、Agaress_Hell 随机候选到攻击成功下游链、Jackie 球体链、sjar 硬币/导弹链、eltis `AI character -> 11101 dummy -> 11100 machine -> 8498 thunder` 分层链、_firewitch/ador 销毁动作创建 common 爆炸并同块召唤 monster 的碰撞 ID 样本、_firewitch/meteo 销毁动作创建 Jeff 下游样本、_cyclone opt action 创建 48117 回同桶 Short1 样本、mesteria `ZPOS <= 5 -> 40005 Common/ShootingStarExp` 条件创建链、at_5t_walker `rocket1 -> rocket2 -> rocketboom` 条件创建链、at_5t_walker `special -> 10268 BombingMissile -> 10284 rocketboom1` 批量投放链、`Exp -> 8715 GiselleFloor` 一跳下游链、上下文 registry 路由、随机行为选择、`[INDEX]` 下随机 passiveobject ID 候选、空 `[INDEX]` 和内联列形检索结论；下一步扩大目录分桶和递归去重。 |
| P1 | 销毁/消失条件 | 已有空块、时间 token、动画结束 token、销毁后创建、生命、碰撞消失、blocking/hp destroy、destroy particle 与 action 播放粒子样本、_bat 攻击成功销毁并由 last action 播放粒子侧车样本、_aganzo action 内 `ON SET ACTION -> time` 与 `time + 4000 <= GET TIME -> DESTROY` 的 owner 未闭合样本、_darkcough `.obj` 直连 ANI 且 `[on end of animation]` 销毁样本、_cypherelec `time + 4000` / `time + 200` action 计时销毁样本、_cyclone `time + 10000` action 计时销毁与 opt action frame 销毁样本、rina action frame 销毁与 `SUMMON MASTER -> SET SPEED` 行为样本、laowu `CHECK TIME 500` + AI CHARACTER 21401 检查失败后自毁样本，以及 owner 未命中 `empty01.act` 的 FRAME1 自毁侧车样本；mesteria 补充 `ZPOS <= 5` 条件创建后同一行为块 `[DESTROY]`、FRAME0/1/2 粒子行为与无对象级销毁块样本；下一步按目录扩大抽样。 |
| P1 | Homing 追踪块 | 已有 `[ENEMY]`、`[MONSTER]`、`[straight homing]` 和可选字段缺省样本；_bat 补充 `[ENEMY]` + velocity/check gap/max rotation 样本；_burnfire 补充 `[ENEMY]` + sync animation rotation `1` + max rotation `360` 样本；_cypherelec 补充 `[ENEMY]` + velocity `250 0` + check gap `100000` + max rotation `360` 样本；_cyclone 补充同桶内 `[ENEMY]` velocity `150 150` / `250 250` 与 check gap `500` / `300` 差异样本；rina 补充 `[ENEMY]` + velocity `10 10` + check gap `100` + sync `1` + max/init/diff rotation `360` + homing time `50000` 样本；下一步按目录扩大抽样。 |
| P1 | AttackInfo 状态/PVP | PVP 40 文件已全量盘点并完成静态 owner 审计；31 个闭合到 owner/ANI 或 owner/action/ANI 观察点，9 个 zrr_skill 文件登记为 owner 未闭合或 ANI 断链风险；_burnfire 补充普通 `.atk` 中 `[active status] [blind]` 与 `[burn]` payload 样本，_cursequeen/spire 补充 curse/slow/armor break 三段 active status 样本，_cursequeen/cry 补充 curse 单段 active status 样本。下一步若要追 zrr 运行来源，应转入 skill/load_state/NUT 或实机链路专题。 |
| P2 | `.ptl/.als` 边界 | 确认它们在本主题中只作为资源引用/侧车，不写客户端资源完整结论。 |

## 验收边界

- 当前账本是静态只读覆盖账本，不等于实机命中、伤害、碰撞、追踪、销毁时序验证。
- 辅助对照只能用于差异提醒；后续任何主目标修改前必须回主目标单独复核。
- Workbench 只保留纯结论，不保留资料来源链、候选报告、旧报告路径或本地证据路径。

